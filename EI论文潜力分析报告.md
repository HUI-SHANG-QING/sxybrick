# SxyBrick 记忆卡片 · EI 论文潜力分析报告

> 生成日期：2026-09-01
> 分析对象：`E:/kcsj2026/card/new_card`（SxyBrick 记忆卡片 v2.0.0，本地优先离线 PWA）
> 分析目标：从技术方案、算法创新、系统架构、实验设计与结果、工程实现、应用价值六个维度，系统梳理具备 EI 论文发表潜力的研究点与创新点；逐方向评估研究价值、与现有工作差异、可开展方法/实验、EI 可行性与难度，并给出优先推荐。
> 方法：代码级取证（`src/algorithms/*`、`src/agent/*`、`package.json` 全量阅读）+ 文献基线检索（FSRS / LLM 闪卡生成 / RAG 教育 / 知识图谱构建 / 个体化间隔重复）确立"现有工作"边界。

---

## 0. 执行摘要（TL;DR）

本项目是一个**本地优先（local-first）、完全离线、可多端同步**的 AI 学习/记忆卡片系统，技术栈为 Vue 3 + Dexie(IndexedDB) + Vite + PWA，算法层以**多信号融合的自适应间隔重复调度**为核心，叠加**溯源式 RAG 闪卡/考题生成**、**教学法知识图谱自动构建**、**到期负载预测**与**错误归因**。

**一句话结论**：本项目**具备 EI 论文潜力**，且不是"套壳 LLM 应用"级别的潜力，而是有**真实算法纵深 + 真实纵向数据 + 完整工程闭环**的潜力。但潜力能否兑现，**关键卡点在"数据规模与评估协议"**，而非算法本身。

**优先推荐（按优先级）**：

| 优先级 | 方向 | 一句话定位 | EI 可行性 | 难度 |
|---|---|---|---|---|
| **P0（首选）** | **多信号个体化自适应间隔重复（A）** | 把 FSRS 从"4 档评分"扩展到"回忆强度×响应时长×连续错误×预测试×休息日"五维信号 + 分科自适应保持率 | ★★★★☆ 高 | 中 |
| **P1（强备选/可并投）** | **溯源式 RAG 生成 + 错误归因闭环（B）** | 每卡可溯源到源文档片段，错误归因驱动重生成/重调度 | ★★★★☆ 高 | 中 |
| **P2（系统型/可独立成篇）** | **教学法知识图谱自动构建 + 掌握度感知规划（C）** | 从闪卡+源文档自动构建"前置/相关"依赖图，多层 BFS 反查未掌握前置 | ★★★☆☆ 中高 | 中高 |
| P3 | 本地优先隐私保护架构（D） | 离线/隐私优先 AI 学习系统架构 | ★★★☆☆ 中 | 中 |
| P4 | 到期负载预测与风险优先级（E，建议并入 A） | 到期洪峰预测 + 净值得分 + 过期卡检测 | ★★☆☆☆ 中低 | 中低 |
| P5 | 黄金时段/校准（F，仅作 A/B 组件） | 复习时段推荐、学习者校准 | ★★☆☆☆ 低 | 低 |

**核心建议**：以 **A（多信号自适应 SRS）** 为主论文方向，**B（溯源 RAG 闭环）** 为第二篇/并投方向，**C（教学法图谱）** 作为 A 的支撑组件或独立系统论文。三者的共同前提是**建立可复现的数据集与评估协议**（见 §5 数据策略）——这是把"工程创新"升级为"论文创新"的唯一门槛。

---

## 1. 项目总览（代码级取证）

### 1.1 定位与技术栈

- **产品**：SxyBrick 记忆卡片 · 本地优先离线版（PWA），`sxybrick-local` v2.0.0。
- **前端**：Vue 3.5 + Pinia + Vue Router + Element Plus + ECharts/ECharts-wordcloud（分析可视化）+ Three.js（3D 图谱）+ KaTeX（公式）。
- **数据层（关键创新点之一）**：**Dexie（IndexedDB）本地优先**，完全离线可用；`sql.js`（WASM SQLite）用于分析/同步；`sync-hub/hub.js` 提供**多端同步**（`npm run hub`）。
- **文档解析（关键创新点之一）**：`pdfjs-dist`（PDF）、`mammoth`（DOCX）、`exceljs`（Excel）、`tesseract.js`（OCR 图片）、`jszip`、`marked`（Markdown）、`highlight.js`、`dompurify`（XSS 净化）——**多格式源文档 → 闪卡/图谱**的完整管线。
- **AI 层**：本地 `llm.js` 抽象（可离线/可接 LLM）、`embedding.js`（向量化）、`retrieval.js`（RAG）、纯逻辑 `retrieval-core.js`（BM25+向量混合打分）。
- **工程/质量**：`node --test`（`tests/*.test.mjs`）、`fake-indexeddb`、`jsdom`、ESLint + Prettier、`check-view-i18n.mjs`（i18n 强校验）、`benchmark.mjs`。**大量模块刻意做成"纯函数、确定性、Node 可直接单测"**（无 IO、无 Dexie 依赖）——这本身就是一项值得在论文里强调的**可验证性工程**（算法可脱离 UI 单测与复现）。

### 1.2 算法层（`src/algorithms/`，16 个模块）

| 模块 | 职责 | 创新密度 |
|---|---|---|
| `srs.js` | **多信号融合记忆曲线调度**（类 SM-2 增强，浏览器端纯 JS） | **高** |
| `fsrs.js` | **FSRS 调度器 + 权重训练循环**（326 行，27 符号，稳定性/不稳定度/间隔） | **高** |
| `forecast.js` | **到期洪峰预测**（Due Forecast，预测复习量峰值） | 中高 |
| `mistakeAttribution.js` | **错误归因**（诊断"为何错"） | **高** |
| `graphAuto.js` | **知识图谱自动构建**（共现 + LLM 抽取 + 边评分，334 行） | **高** |
| `graph-resolve.js` | **图谱 ID 空间统一解析层**（处理两种端点写法混存的兼容） | 中 |
| `prereq.js` | **多层前置依赖 BFS 回溯**（反查目标卡所有未掌握祖先前置） | **高** |
| `adaptive-retention.js` | **分科自适应目标保持率**（掌握度低→保持率高→间隔短） | 中高 |
| `pretest.js` / `calibration.js` / `calibration-feedback.js` | 预测试稳定性、学习者校准与反馈 | 中 |
| `golden-hours.js` | 黄金时段（24h 复习分布找最优连续窗口） | 低 |
| `networth.js` | 卡片"净值得分"（风险/价值优先级） | 中 |
| `session.js` / `scheduling.js` | 会话与排程编排 | 中 |
| `mindmap-graph.js` | 思维导图图谱 | 中 |
| `source-trace.js` | **溯源**（闪卡→源文档片段，可验证性） | **高** |
| `fsrs-benchmark.js` | **基准评测框架**（对标基线） | 中高（论文资产） |

### 1.3 智能体层（`src/agent/`，18+ 模块）

`orchestrator.js`（编排）、`pipeline.js`（生成管线）、`registry.js` + `tools/`（工具注册/调用）、`blackboard.js`（共享状态黑板）、`proactive.js`（主动建议）、`context.js`（上下文）、`memory.js`（记忆）、`retrieval.js`/`retrieval-core.js`（RAG 检索 + 纯逻辑混合打分）、`attribution.js`（归因）、`analytics.js`/`examAnalytics.js` + `analytics.worker.js`（分析/考试分析，Worker 化）、`stale.js`（过期卡检测）、`embedding.js`、`llm.js`、`agents/`（base/index）。

**架构特征**：黑板 + 工具注册 + 管线的**可编排多阶段 Agent 系统**，且关键评分逻辑（`retrieval-core.js`、`prereq.js`、`adaptive-retention.js`、`golden-hours.js`）全部抽成**无 IO 纯函数**——这是"系统 + 可复现实验"两全的工程底座。

### 1.4 一句话技术画像

> 一个**"多信号自适应 SRS 引擎 + 溯源 RAG 生成 + 教学法图谱规划"** 三位一体的**本地优先离线 AI 学习系统**，算法层刻意纯函数化以保证可单测、可复现，并自带基准评测框架。

这个画像意味着：它**不止是"接了 LLM 的 App"**，而是有**可量化的算法贡献面**（调度/预测/归因/图谱质量）和**真实纵向行为数据**（复习日志、评分、响应时长、错误分布）——这正是论文区别于"应用演示"的两个支柱。

---

## 2. 候选方向逐一分析

> 每个方向按统一结构展开：**① 创新点（代码取证）** → **② 研究价值** → **③ 与现有工作差异** → **④ 可开展的方法与实验** → **⑤ EI 可行性与难度评估**。可行性用星级（★1~5），难度分低/中/中高/高。

### 方向 A（P0，首选）：多信号个体化自适应间隔重复调度（Multi-Signal Individualized Adaptive SRS）

**① 创新点（代码取证）**
核心在 `srs.js`（多信号融合记忆曲线，类 SM-2 增强）+ `fsrs.js`（FSRS 调度器 + **权重在线训练循环**）+ `adaptive-retention.js`（**分科自适应目标保持率**）+ `pretest.js`/`calibration.js`。

关键差异信号——FSRS/SM-2 的输入是**离散 4 档评分**（Again/Hard/Good/Easy）+ 已过时间；本项目在调度决策中**额外注入了多维连续/行为信号**：
- **回忆强度（retrieval_strength）**：把"记得多牢"建模为连续量而非 4 档；
- **连续错误（streak of consecutive failures）**：短期错误序列直接压制间隔、触发前置回溯（联动 `prereq.js`）；
- **响应时长（response time / RT）**：回忆所用的时间作为置信度/难度信号（答对但慢 ≠ 答对且快）；
- **预测试稳定性（pretest stability）**：`pretest.js` 在正式复习前用轻量探测估计"当前稳定度"，用于校正首次/回归复习的基线间隔；
- **休息日（rest/break days）**：`srs.js` 感知学习中断/回归，回归时重估而非简单沿用旧间隔；
- **分科自适应保持率**：`adaptive-retention.js` 让 `desiredRetention` 成为**掌握度的函数**（掌握度 0 → 0.95 最勤；100 → 0.80 最松），并对"无数据科目"做了正确的兜底（不把零复习当零掌握）。

即：**把 FSRS 的"评分驱动"升级为"多信号驱动的个体化调度"，并把目标保持率本身也个体化（按科）**。这是本方向最大的、且**文献中明确被标记为开放问题**的切入口。

**② 研究价值**
- 直击**个体化（individualization）缺口**：现有 SRS 对"同样答 Good，但一个 2 秒答、一个 20 秒答；一个连错 3 次、一个偶错"几乎一视同仁，而认知科学证据表明记忆痕迹强度与检索耗时、错误模式强相关。
- **信号可扩展性**：五维信号是"信号空间"的一个实例，可论证该框架可继续吸收更多行为特征（作答正确性分布、来源难度、时间戳节律），形成**统一的信号融合调度范式**。
- **可落地**：算法纯 JS、浏览器端实时运行，无需服务器训练——**隐私友好 + 低延迟**，是"个体化学习系统"里稀缺的端侧可行性。
- **有纵向数据**：复习日志天然累积了评分+RT+时间戳序列，可直接做**离线回放/反事实评估**，不需要重新收集。

**③ 与现有工作差异（文献基线）**
| 工作 | 输入信号 | 个体化 | 本项目差异 |
|---|---|---|---|
| **SM-2**（Wozniak 1990） | 5 档质量 + 易度因子 | 无（易度为全局因子） | 多维连续信号 + 分科保持率 |
| **FSRS / FSRS-4.5**（arXiv 2402.08964） | 离散 4 档评分 + 已过时间，模型稳定性/难度 | 权重可由语料拟合，但**信号维度固定** | **扩展信号空间**（RT/连续错/预测试/休息日）+ **desiredRetention 个体化** |
| **FSRS-5**（arXiv 2501.08582） | 同上 + 更多特征工程 | 同上 | 与 FSRS-5 特征工程**正交**：本项目强调**端侧实时 + 多行为信号 + 分科自适应保持率**，并带**溯源闭环**（方向 B） |
| **个体化遗忘/学习者差异**（arXiv 2502.05846、2410.14046） | 多为离线建模/模拟器 | 提出个体化**必要性**，少给**在线可部署方案** | 本项目给**端侧可部署的在线方案** + 真实纵向数据验证 |
| **RT 作为记忆信号**（部分 SRS 研究） | 单用 RT | 少 | 把 RT **与错误模式、预测试、休息日联合**进 FSRS 框架，非单信号 |

**定位句（可作为论文 Contribution 1）**：*We extend the FSRS scheduling model from discrete rating-driven to a continuous, multi-signal individualized scheduler that ingests retrieval strength, response time, consecutive-error streaks, pre-test stability and rest-day context, and makes the target retention itself an adaptive function of per-subject mastery—deployed fully on-device.*

**④ 可开展的方法与实验**
- **基线对照**：本项目调度器 vs **SM-2** vs **原始 FSRS-4.5/5**（固定全局 retention）vs **仅评分版**（消融：关掉 RT/连续错/预测试/休息日各信号，逐一验证边际贡献）。
- **评估指标**：
  - 离线：**预测回忆准确率 vs 实际结果**的校准误差（Brier/log-loss）、**间隔-保持率 Pareto 前沿**（在达到目标保持率下平均间隔最大化 = 省时）、**权重收敛稳定性**（训练循环对语料扰动的鲁棒性）。
  - 在线/真实数据：**实际回忆正确率**、**单卡复习次数下降**、**遗忘事件数下降**、**单位复习时间的"有效记忆"提升**。
- **消融**：5 个信号逐个 on/off，证明 RT 与"连续错"是主要增益来源（预判结论）。
- **个体化有效性**：分"高 RT 变异"与"低 RT 变异"学习者子群，验证多信号版对高变异子群增益更大（支撑"个体化"主张）。
- **可复现性**：把 `fsrs-benchmark.js` 升级为论文级 benchmark（固定随机种子、公开数据子集、固定评估脚本）——**纯函数模块让这一步成本极低**。

**⑤ EI 可行性与难度**
- **可行性 ★★★★☆（高）**：算法贡献清晰、有可量化指标、有真实纵向数据、有基线可对标、端侧可复现。**EI（如《计算机工程与应用》、《现代电子技术》、《计算机应用研究》及 EI 检索的国际会议/期刊）对"自适应算法 + 系统 + 实验"组合接受度高**，且无需顶会级的 SOTA 碾压，**"方法改进 + 系统 + 充分实验"**即可。
- **难度 中**：主要成本在**数据整理 + 评估协议搭建**（见 §5），算法本体已在代码中，**增量小**。最大风险是**数据量/学习者多样性**——若只有单一用户日志，"个体化"主张会被审稿人质疑外推性，需要补充多用户数据或用公开 SRS 语料（如开源 FSRS 评测集）做对照。
- **结论**：**首选方向**。建议作为**主论文**，B 作为第二篇/同系统另一贡献线，C 作为支撑。

---

### 方向 B（P1，强备选/可并投）：溯源式 RAG 闪卡/考题生成 + 错误归因闭环（Grounded RAG Card Generation + Mistake-Attribution Loop）

**① 创新点（代码取证）**
核心在 `src/agent/retrieval.js` + `retrieval-core.js`（**BM25 + 向量混合检索**，打分/融合抽成无 IO 纯函数）+ `chunkText`（`CHUNK_LEN`/`CHUNK_OVERLAP` 可控分块）+ `source-trace.js`（**闪卡→源文档片段溯源**）+ `mistakeAttribution.js`（**错误归因**）+ `attribution.js`（Agent 层归因编排）+ `pipeline.js`（多阶段生成管线）。

两个被很多"LLM 出题"项目**忽视但论文里极值钱**的工程/算法设计：
- **溯源（grounding/provenance）**：每张生成的闪卡/考题都**绑定到源文档的具体片段**（`source-trace.js`），使生成结果**可验证、可追溯、可反查**。这直接回应了 LLM 教育内容生成里最大的学术批评——**幻觉与不可验证**。
- **错误归因闭环（attribution-driven loop）**：`mistakeAttribution.js` 不是简单地"答错→重背"，而是**诊断"为何错"**（概念缺口/记忆衰退/审题错误/前置未掌握等），并据此**驱动重生成（换一种问法/补前置）与重调度（联动方向 A 的连续错信号 + 方向 C 的前置回溯）**。这是一个**生成—检验—归因—再干预**的闭环，而非单向生成。

**② 研究价值**
- **幻觉治理**：溯源是教育场景 LLM 内容可信度的关键，且**有可量化指标**（溯源准确率、片段-卡片对齐度、无幻觉率）。
- **闭环 vs 单向**：多数 LLM 出题工作是"给材料→出题→完"，本项目的**归因驱动的二次干预**是更强的系统级贡献，可论证"生成质量"与"学习有效性"的联动。
- **混合检索**：BM25+向量的混合打分是 RAG 标准做法，但其**在"短卡片/术语密集"学习语料上的配比调优**（学习文本短、术语多、语义近邻易混）是一个**可实证的小而实**的点。
- **数据友好**：生成管线天然产出"源片段↔卡片"配对，**是现成的标注语料**，可训练/评测对齐模型。

**③ 与现有工作差异（文献基线）**
| 工作 | 关注点 | 溯源 | 闭环归因 | 本项目差异 |
|---|---|---|---|---|
| **FlashcardQA**（arXiv 2408.11792） | 用 LLM 生成闪卡/问答数据集 | 数据集级，非逐卡溯源 | 无 | **逐卡溯源 + 归因闭环** |
| **LLM-QG**（arXiv 2504.16909，问题生成） | LLM 出题质量 | 多无 | 无 | 归因驱动重生成 |
| **GraphRAG**（arXiv 2404.16130） | 图谱增强的全局检索 | 图谱溯源 | 无 | 面向**短卡片/学习语料**的混合检索 + **生成侧溯源**（GraphRAG 偏检索侧） |
| **RAG+教育/ITS**（多篇 2024-25） | 用 RAG 给 ITS 注入知识 | 少 | 少 | **生成-归因-干预闭环**，且**端侧可部署** |

**定位句（Contribution 1）**：*We propose a grounded RAG pipeline for study-card and item generation in which every generated artifact is traceable to source spans, and a mistake-attribution loop that re-generates or re-schedules cards based on diagnosed error causes—closing the loop between generation quality and learning efficacy, deployed on-device.*

**④ 可开展的方法与实验**
- **生成质量**：
  - **忠实度/幻觉率**：抽样卡片，人工/LLM 裁判判定"答案是否被源片段支持"（faithfulness），对比"有溯源 vs 无溯源"版本。
  - **溯源准确率**：溯源片段的 Top-k 召回（人工标注正确片段）。
  - **多样性/难度分布**：生成卡的题型/难度分布合理性。
- **检索消融**：纯向量 vs 纯 BM25 vs **混合**（本项目），在"术语密集短文本"上测 **MRR / nDCG / 忠实度**，论证混合在短文本上的优势。
- **闭环有效性**：对比"一次性生成" vs "归因驱动再生成"，在**学习者重做正确率提升**上比较——这是把"生成质量"落到"学习效果"的关键实验。
- **可复现性**：`retrieval-core.js` 是纯函数，检索/打分可**确定性复现**，利于审稿人复算。

**⑤ EI 可行性与难度**
- **可行性 ★★★★☆（高）**：LLM+教育是当前 EI 热门方向，**"溯源可信 + 闭环归因"**是清晰的差异化贡献，且实验设计（忠实度、溯源召回、闭环前后对比）标准、可执行。**比方向 A 更"新热点"，但"个体化算法纵深"略弱**。
- **难度 中**：主要成本在**构建人工/裁判评估集**（忠实度、溯源标注）+ 归因类别的**人工标定**。需要一定规模的人工评估（几百卡级别即可），成本可控。风险：LLM 裁判的**可靠性**需说明（可用双裁判 + 人工抽检校准）。
- **结论**：**强备选**，与 A **可并投**（A 偏"自适应算法"，B 偏"可信生成+闭环系统"），共享同一系统底座与数据，**边际成本低**。

---

### 方向 C（P2，系统型/可独立成篇）：教学法知识图谱自动构建 + 掌握度感知的前置规划（Pedagogical KG Auto-Construction + Mastery-Aware Prerequisite Planning）

**① 创新点（代码取证）**
核心在 `src/algorithms/graphAuto.js`（**334 行，自动构建图谱**：共现统计 + LLM 关系抽取 + **边评分**）+ `graph-resolve.js`（**ID 空间统一解析层**，处理"from/to 是卡片 id"与"端点是文本、id 在 fromCardId/toCardId"两种混存写法——这是真实数据里的脏数据治理）+ `prereq.js`（**多层前置依赖 BFS 回溯**：从目标薄弱卡向上收集**所有层级**的未掌握祖先前置，visited 防环；related 保持单层避免范围膨胀）+ `mindmap-graph.js`（思维导图）。

两个有论文含金量的设计：
- **从"闪卡+源文档"自动抽取"教学法关系"图谱**：不是通用 KG（实体-关系-实体），而是面向**学习路径**的**前置/依赖（prereq）**与**相关（related）**两类**有语义权重**的关系，且**边带评分**（置信度/强度），可据此排序与剪枝。
- **掌握度感知的规划（mastery-aware planning）**：`prereq.js` 把"图谱 + 已掌握集合"作为输入，**自动反查目标卡缺哪些前置**，并据此**把未掌握前置纳入练习集/学习计划**——即"**先补地基再盖楼**"。这是一个**图算法 + 掌握状态**驱动的**自适应学习路径规划**问题。

**② 研究价值**
- **教学法图谱 ≠ 通用 KG**：通用 KG 构建（GraphRAG、文本三元组抽取）已有大量工作，但**面向"学习顺序/前置依赖"的图谱**在**个人化、从学生自己的卡片/笔记**自动构建的，**文献极少**——这是**领域切分上的空白**。
- **脏数据治理的真实工程价值**：`graph-resolve.js` 处理的两套 ID 空间混存，是**真实多来源图谱合并**的缩影，可作为"图谱数据质量/统一"的一个实证案例。
- **规划可量化**：前置规划的效果可度量（补前置后目标卡正确率提升、错题轰炸率下降），与方向 A（连续错信号）**强耦合**——**图谱给出"补什么"，A 给出"何时补"，B 给出"补的内容是否可信"**，三者构成完整闭环。
- **可解释性**：图谱本身是**可解释的学习路径**（可视化 + 可反查依据），对 ITS 研究有吸引力。

**③ 与现有工作差异（文献基线）**
| 工作 | 图谱来源 | 关系类型 | 规划 | 本项目差异 |
|---|---|---|---|---|
| **通用 KG 构建**（GraphRAG 2404.16130 等） | 长文档 | 通用实体-关系 | 检索增强 | 面向**学习路径**的**前置/相关**语义关系 + **掌握度感知规划** |
| **教育知识图谱（预定义）** | 专家/教材大纲 | 前置依赖 | 学习路径推荐 | 本项目**从学生自有卡片+源文档自动构建**，无需专家标注 |
| **ITS 知识追踪（KT，如 DKT/AKT）** | 序列 | 隐式技能掌握 | 下一题预测 | 本项目是**显式图结构 + 掌握状态 + 前置补全**，可解释、可编辑 |

**定位句（Contribution 1）**：*We automatically construct a pedagogical knowledge graph of prerequisite/related dependencies from a learner's own flashcards and source documents, with scored edges, and provide a mastery-aware prerequisite planner that back-traces all unmastered ancestors to close gaps before tackling weak concepts.*

**④ 可开展的方法与实验**
- **图谱构建质量**：
  - **边精确率/召回率**：抽样边，人工判定"前置/相关"标注与边评分是否合理（LLM 抽取 vs 共现统计 vs 混合）。
  - **图谱连通性/规模**：节点/边分布、环检测、孤儿节点比例。
  - **ID 空间统一的有效性**：`graph-resolve.js` 修复前后"前置回溯静默失效"的比例变化（一个**可讲的小实证**）。
- **规划有效性**：
  - **前置回溯正确率**：给定薄弱卡，回溯出的前置集合与人工标注的"真正前置"重合度。
  - **学习效果**：对比"直接练薄弱卡" vs "先补回溯出的前置再练"，在**目标卡正确率**、**后续相关卡遗忘率**上比较（**与方向 A/B 共享数据**）。
  - **避免"错题轰炸"**：量化"未补前置直接练"导致的重复错误率下降。
- **可复现性**：`prereq.js`/`graphAuto.js` 核心为纯函数/确定性，规划结果可复算。

**⑤ EI 可行性与难度**
- **可行性 ★★★☆☆（中高）**：作为**系统/应用型 EI** 论文（"我们构建了一个自动教学法图谱+规划的学习系统，并做了实证"）**可行**；作为**纯算法 SOTA** 论文偏弱（前置规划问题规模与难度不如 A/B 的算法纵深）。**最佳定位是"系统 + 实证"型**，或与 A 合并成"一个系统三贡献"的长文。
- **难度 中高**：成本在**图谱边的人工标注评估集**（需一定规模，几百条边）+ **学习效果实验**（需学习者数据，与 A 共享）。**风险**：前置关系**高度学科依赖**，泛化性论证需要跨学科数据；"从卡片自动构建"的**噪声**需说明（共现 ≠ 前置，需 LLM 判别 + 评分剪枝）。
- **结论**：**强支撑 / 可独立系统型论文**。建议**作为方向 A 的第二贡献**（"我们的个体化调度器 + 图谱前置规划"），或独立投**系统/应用类** EI。

---

### 方向 D（P3，系统型/独立成篇）：本地优先、隐私保护的离线 AI 学习系统架构（Local-First, Privacy-Preserving Offline AI Learning System）

**① 创新点（代码取证）**
整条技术栈即贡献：**Dexie/IndexedDB 本地优先**（数据不出设备）+ **完全离线 PWA**（`vite-plugin-pwa`）+ **多格式文档本地解析**（PDF/DOCX/Excel/OCR）+ **端侧 LLM 抽象**（`llm.js`，可离线/可接）+ **`sql.js` WASM SQLite**（本地分析/聚合）+ **`sync-hub`**（**用户自托管的多端同步**，非云中心化）+ Worker 化分析（`analytics.worker.js`，不阻塞 UI）+ `dompurify`（XSS 净化）+ `check-view-i18n`（i18n 强校验）。

核心主张：**"个体化 AI 学习系统可以在**不上传数据、不依赖云端**的前提下实现"**——把 A/B/C 三个算法贡献**全部放在端侧**运行。这是**隐私 + 数据主权 + 可离线**的系统级差异化。

**② 研究价值**
- 回应教育/学习数据**敏感**（学习行为=隐私）的现实：端侧 = 数据主权，是**可信学习系统**的稀缺属性。
- **多端同步**（自托管 hub）是**去中心化**路线，区别于"云同步 SaaS"。
- 可作为 A/B/C 的**统一系统叙事**（"我们的贡献不是三个孤立算法，而是一个**本地优先的可信学习系统**"）。

**③ 与现有工作差异**
- 多数 LLM 教育/ITS 系统是**云中心化**（数据上传）；本项目是**端侧 + 自托管同步**。文献中"local-first + LLM + 学习"的**完整系统**极少。
- 与一般"离线 App"差异：不只是离线，而是**离线 + 个体化算法 + 图谱 + RAG + 溯源**全部端侧。

**④ 可开展的方法与实验**
- **架构评估**：端侧 vs 云端的**隐私性**（数据流分析）、**延迟**（端侧 LLM 推理/调度耗时 vs 云端）、**离线可用性**、**同步一致性**（hub 多端冲突处理）。
- **性能**：Worker 化分析对 UI 主线程的阻塞影响、大语料下 Dexie 查询性能、`sql.js` 聚合性能。
- **可复现性/系统演示**：完整的系统级 case study + 隐私威胁模型（threat model）描述。

**⑤ EI 可行性与难度**
- **可行性 ★★★☆☆（中）**：作为**系统/架构型 EI** 可行（"我们设计并实现了一个本地优先可信学习系统，并给出隐私/性能评估"）。**但单独成篇偏弱**——架构本身无"算法纵深"，需靠"隐私 + 性能 + 多端一致性"的实证撑起来。**最佳用法是作为 A/B/C 的系统层统一叙事**，而非独立主打。
- **难度 中**：成本在**隐私威胁模型 + 性能/同步实验设计**。风险：EI 审稿人可能认为"架构"贡献偏工程、创新密度低。
- **结论**：**强支撑 / 系统层统一叙事**。建议**并入 A（主论文的系统部分）**或独立投**系统/应用类** EI，不单独作为算法主打。

### 方向 E（P4，建议并入 A）：到期负载预测与风险优先级（Due-Load Forecast + Risk-Prioritized Scheduling）

**① 创新点（代码取证）**
`forecast.js`（**到期洪峰预测**：预测未来复习量峰值/洪峰）+ `networth.js`（**卡片"净值得分"**：风险/价值综合打分，用于优先级）+ `stale.js`（**过期卡检测**：识别"应复习而未复习"的卡）。

价值：把"复习调度"从"逐卡间隔"**提升到"整体负载管理"**——预测"哪天会爆发到期洪峰"、哪些卡**风险最高（即将遗忘）**、哪些**最该优先**。这是**系统级负载/优先级**视角，而非单卡视角。

**② 与现有工作差异**
- 多数 SRS 是**单卡局部调度**；本方向的**全局负载预测 + 风险优先级**是**运筹/调度**视角（类似"任务调度/负载均衡"）。文献中"SRS 全局负载/洪峰预测"**少见**。
- **可并入 A**：负载预测可作为"多信号调度"的**系统层优化目标**（在个体化调度之上，再做**全局错峰/优先级**）。

**④ 可开展的方法与实验**
- **预测准确性**：到期洪峰预测 vs 实际到期量的误差（MAE/MAPE）。
- **优先级有效性**：风险优先级排序 vs 朴素时间排序，在"遗忘事件数"、"洪峰削平程度"上比较。
- **负载平滑**：错峰策略下的"日复习量方差"下降（削峰填谷）。

**⑤ EI 可行性与难度**
- **可行性 ★★☆☆☆（中低，独立成篇偏弱）**：作为**独立算法**贡献偏弱（预测/优先级相对常规），但**并入 A 后**能显著增强"系统完整性"叙事。
- **难度 中低**：实现已在代码中，主要成本在**预测评估数据**。
- **结论**：**不建议独立成篇**，**建议并入方向 A** 作为"全局负载/优先级"子贡献（增强系统完整性）。

### 方向 F（P5，仅作 A/B 组件）：黄金时段推荐 + 学习者校准（Golden-Hour Scheduling + Learner Calibration）

**① 创新点（代码取证）**
`golden-hours.js`（从 24h 复习分布找**最优连续窗口**，环形跨午夜）+ `pretest.js`/`calibration.js`/`calibration-feedback.js`（**学习者校准**：估计/校正学习者的评分偏差，"自评偏高/偏低"）。

价值：**校准（calibration）**是真有价值的点——学习者自评质量分**系统性偏差**（自信过度），校准能提升评分信号的可靠性（**直接提升方向 A 的信号质量**）。黄金时段相对常规（行为节律推荐）。

**③ 与现有工作差异**
- **校准**：文献中"自评分校准"在 SRS 里**少见**，是有潜力的**信号质量**改进。
- **黄金时段**：偏常规，作为**辅助功能**而非主打。

**④ 可开展的方法与实验**
- **校准有效性**：校准前后"预测准确率"提升（Brier 下降）；"高偏差 vs 低偏差"学习者子群的增益对比。
- **黄金时段**：在推荐时段复习 vs 非推荐时段的**次日的记忆保持率**对比（需纵向数据，样本量是关键）。

**⑤ EI 可行性与难度**
- **可行性 ★★☆☆☆（低，独立成篇偏弱）**：单独成篇贡献密度不足。
- **难度 低**：实现已在代码中。
- **结论**：**不独立成篇**。**校准**建议**并入 A**（作为"评分信号可靠性"子贡献，提升 A 的信号质量论证）；**黄金时段**作为**系统功能**提及即可。

---

## 3. 综合排序与优先推荐

### 3.1 横向总排序

| 排名 | 方向 | 定位 | 算法纵深 | 数据需求 | EI 可行性 | 难度 | 推荐用法 |
|---|---|---|---|---|---|---|---|
| **1** | **A 多信号个体化自适应 SRS** | 主打算法 | **高** | 中（纵向日志） | **★★★★☆** | 中 | **主论文** |
| **2** | **B 溯源 RAG + 归因闭环** | 主打算法/系统 | 中高 | 中（人工评估集） | **★★★★☆** | 中 | **第二篇/并投** |
| **3** | **C 教学法图谱 + 前置规划** | 系统/算法混合 | 中 | 中高（边标注） | ★★★☆☆ | 中高 | **A 的第二贡献 / 独立系统篇** |
| **4** | **D 本地优先隐私架构** | 系统 | 低 | 低（性能/隐私实验） | ★★★☆☆ | 中 | **A 的系统层统一叙事** |
| **5** | **E 到期负载预测 + 优先级** | 系统/运筹 | 中低 | 中 | ★★☆☆☆ | 中低 | **并入 A** |
| **6** | **F 黄金时段 + 校准** | 组件 | 低（校准中） | 低/中 | ★★☆☆☆ | 低 | **校准并入 A，黄金时段作功能** |

### 3.2 推荐组合（论文打包方案）

**方案 ①（最稳，单篇主打）**——**A 为主 + C/D/E/F 为支撑的"长文系统+算法"篇**
- **标题方向**：*On-Device Multi-Signal Individualized Spaced Repetition with Pedagogical Knowledge-Graph Planning and Grounded RAG Generation*（或中文：*端侧多信号个体化间隔重复：融合教学法图谱规划与溯源式 RAG 生成的本地优先学习系统*）。
- **结构**：
  1. 引言：个体化 + 隐私 + 可信三大痛点。
  2. **贡献 1（主打）= A**：多信号个体化调度器 + 分科自适应保持率（含消融）。
  3. **贡献 2 = C**：教学法图谱自动构建 + 掌握度感知前置规划。
  4. **贡献 3 = B**：溯源 RAG 生成 + 错误归因闭环（把"补的前置/重生成的卡"落到可信内容）。
  5. **系统 = D**：本地优先架构 + 隐私威胁模型。
  6. **增强 = E/F**：全局负载错峰 + 评分校准（作为系统完整性与信号可靠性小节）。
- **优点**：**叙事完整**（一个系统、三贡献、强闭环）、**单篇信息量大**、EI 长文/系统类友好；**共享同一数据集**，边际成本最低。
- **缺点**：篇幅大，需**统一评估协议**贯穿三贡献。

**方案 ②（最聚焦，单篇算法）**——**只做 A**
- **标题方向**：*Multi-Signal Individualized Scheduling: Extending FSRS with Continuous Behavior Signals and Adaptive Target Retention*。
- **优点**：**最聚焦、最易写好**、算法纵深最清晰、消融最干净；**EI 命中率最高**（"方法改进 + 充分实验"型，审稿标准明确）。
- **缺点**：少了"系统/可信"的差异化亮点（但 A 本身已足够强）。

**方案 ③（双篇并投）**——**A 一篇 + B 一篇**
- **A 篇**：个体化自适应调度（算法）。
- **B 篇**：溯源 RAG + 归因闭环（可信生成系统）。
- **优点**：**两篇都强、互不抢贡献**；A 偏"自适应算法"、B 偏"可信 LLM 系统"，**投稿面更宽**（一篇投算法/教育技术类 EI，一篇投 LLM/NLP/智能体类 EI）。
- **缺点**：工作量翻倍，需**两套评估集**（调度日志 + 生成人工评估）。

**最终推荐**：**首选「方案 ②（A 单篇主打）」** 作为第一篇（命中率最高、最易写好）；**A 发表/投稿后，以「方案 ③」的第二篇推 B**；**C 视数据规模**，要么并入 A 方案①、要么独立投系统类 EI。**理由**：A 的算法纵深、可量化指标、真实纵向数据、纯函数可复现性，是**风险最低、确定性最高**的 EI 路径；B 紧随其后作为"可信生成"热点的第二篇；C/D/E/F 作为支撑组件，**不单独消耗投稿额度**。

### 3.3 为什么是 A 第一（核心理由）
1. **文献明确留出缺口**：FSRS/FSRS-5 信号维度固定、desiredRetention 全局；"个体化在线可部署方案"被多篇标为开放问题——**A 正踩在缺口上**。
2. **可量化、可消融、可复现**：调度器是纯函数，`fsrs-benchmark.js` 已就位，**评估协议搭建成本极低**（其他方向的人工评估集成本更高）。
3. **真实纵向数据现成**：复习日志天然含评分+RT+时间戳，**无需重新收集**即可做离线回放/反事实评估。
4. **端侧可行性是稀缺卖点**：浏览器端实时、无需服务器训练，**隐私友好**——与 D 形成天然呼应。
5. **审稿标准明确**：EI 对"自适应算法改进 + 基线对照 + 消融 + 系统"接受度高，**无需顶会级 SOTA 碾压**。

---

## 4. 实验设计与结果策略（把"工程创新"升级为"论文创新"的唯一门槛）

> **核心判断**：本项目的算法本体**已在代码中**，论文成败**不在算法，而在数据规模与评估协议**。以下是把潜力兑现的具体策略。

### 4.1 数据策略（最关键）
- **主数据 = 本项目真实复习日志**（评分、RT、时间戳、连续错、科目）。**必须**做：
  - **脱敏 + 多学习者**：单用户日志会致命质疑"个体化外推性"。**目标 ≥ 10~30 名学习者、跨 ≥ 2 个学科**（最低门槛）。若真实用户不足，**补充公开 SRS 语料**（开源 FSRS 评测集）做**算法层对照**，把"个体化"与"算法有效性"分离论证。
  - **固定评估协议**：固定随机种子、固定数据切分（按时间/按用户防泄漏）、固定指标脚本——**纯函数模块让这一步几乎零成本**（`fsrs-benchmark.js` 升级为论文级 benchmark 即完成）。
- **B 的评估集**：**几百卡级别**的"源片段↔卡片"人工标注（忠实度、溯源正确性、归因类别）即可，成本可控。
- **C 的评估集**：**几百条边**的人工标注（前置/相关正确性 + 边评分合理性）。

### 4.2 指标体系（与方向对应）
| 方向 | 核心指标 | 基线 |
|---|---|---|
| **A** | 校准误差（Brier/log-loss）、间隔-保持率 Pareto 前沿、实际回忆正确率、单卡复习次数↓、遗忘事件↓ | SM-2、FSRS-4.5/5（固定 retention）、仅评分版（消融） |
| **B** | 忠实度/幻觉率、溯源 Top-k 召回、混合检索 MRR/nDCG、闭环前后重做正确率↑ | 纯向量、纯 BM25、一次性生成 |
| **C** | 边精确率/召回率、前置回溯正确率、补前置后目标卡正确率↑、错题轰炸率↓ | 无规划（直接练）、单层回溯、通用 KG 检索 |
| **D** | 隐私（数据流/威胁模型）、端侧 vs 云端延迟、同步一致性、Worker 主线程阻塞 | 云中心化基线 |
| **E** | 洪峰预测 MAE/MAPE、优先级 vs 时间排序的遗忘数↓、日复习量方差↓（削峰） | 朴素时间排序 |
| **F** | 校准前后 Brier↓、推荐时段次日保持率↑ | 未校准、随机时段 |

### 4.3 评估方法学（防审稿人质疑）
- **离线回放/反事实评估**：在历史日志上"如果用调度器 X，间隔/结果会如何"，避免在线 A/B 的伦理/成本。
- **防数据泄漏**：按**时间**（训练早、测试晚）或**用户**（训练集用户、测试集用户）切分，**严禁**同用户跨集。
- **LLM 裁判可靠性**（B）：双裁判 + 人工抽检校准报告 Kappa。
- **显著性检验**：配对 t 检验 / Wilcoxon，报告效应量（Cohen's d），不只 p 值。
- **可复现包**：公开 benchmark 脚本 + 数据子集 + 固定种子 + 纯函数模块，**直接可跑**。

---

## 5. 风险与缓解

| 风险 | 影响 | 缓解 |
|---|---|---|
| **数据量/学习者多样性不足** | "个体化"外推性被质疑（**最大风险**） | 多学习者脱敏数据 + 公开 SRS 语料做算法层对照；把"个体化"与"算法有效性"分离论证 |
| **LLM 裁判不可靠**（B） | 忠实度/溯源评估被质疑 | 双裁判 + 人工抽检 + 报告一致性 |
| **前置关系学科依赖**（C） | 图谱泛化性被质疑 | 跨 ≥ 2 学科数据；说明"共现≠前置"的噪声与评分剪枝 |
| **架构创新密度低**（D） | 系统篇被认为偏工程 | D 不单独主打，作为 A 的系统层叙事 |
| **与 FSRS-5 撞车**（A） | 创新点被指"特征工程重复" | 强调**端侧实时 + 多行为信号 + 分科自适应保持率 + 溯源闭环**的组合正交性，而非单信号 |
| **纯 JS 性能/可复现性** | 审稿人质疑复现 | 纯函数模块 + benchmark 脚本 + 固定种子，**直接可跑**（反而是卖点） |

---

## 6. 结论

- 本项目**具备真实 EI 论文潜力**，且是**"有算法纵深 + 有纵向数据 + 有工程闭环"**的潜力，而非套壳应用。
- **首选方向 = A（多信号个体化自适应间隔重复）**，作为**第一篇主打**（命中率最高、风险最低）。
- **第二篇 = B（溯源 RAG + 归因闭环）**，作为"可信生成"热点的并投。
- **C（教学法图谱+规划）作为 A 的第二贡献或独立系统篇**；**D/E/F 作为支撑组件**，不单独消耗投稿额度。
- **唯一门槛是数据与评估协议**：补多学习者脱敏数据 + 固定评估协议 + 公开可复现 benchmark，即可把潜力兑现。
- **最快落地路径**：先用**方案 ②（A 单篇）** 把 benchmark 与消融跑出来投稿，同时并行积累 B 的人工评估集，为第二篇铺路。

> 附：本报告全部结论均基于**代码级取证**（`src/algorithms/*`、`src/agent/*`、`package.json` 全量阅读）与**文献基线检索**（FSRS-4.5 arXiv 2402.08964、FSRS-5 arXiv 2501.08582、FlashcardQA arXiv 2408.11792、LLM-QG arXiv 2504.16909、GraphRAG arXiv 2404.16130、个体化间隔重复 arXiv 2502.05846/2410.14046）。建议投稿前对 A 的"多信号调度"与 B 的"归因闭环"做**代码-论文一致性核对**，确保论文声称的能力与实现严格一致（避免审稿人复现翻车）。


