node_modules
dist
dist_bak_*
dev-dist
outputs
# 含 .backup-* 备份，避免 22MB 数据文件误提交（gitignore 不支持行内注释，注释必须独占一行）
sync-hub/hub-data*.json
sync-hub/hub-token.txt
sxybrick-测试数据包-*.json
.DS_Store
*.log
.trae/
.npm-cache/
# 本地 git 事故备份（本轮事故遗留，无恢复价值）
.git.bak-*
# 工具本地数据（会话记忆 / 交付概览，含本机绝对路径，不入公开仓库）
.workbuddy/
