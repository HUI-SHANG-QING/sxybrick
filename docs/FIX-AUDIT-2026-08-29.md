# 08-29 全面审计修复闭环报告（2026-09-03）

> 基线：`docs/AUDIT-2026-08-29.md`（严重 15 / 中等 15 / 建议 20）
> 期间代码经 round10~19 与多轮提交大幅演进，本轮开工前逐项对照**当前源码**复核，
> 只修复"仍然存在"的缺陷；已由历史提交闭环的项（M2/hub stale、M5 主体、
> R1 ErrorBoundary 本体、CardModal 保存锁等）在表中标注"复核已修"。
> 测试基线：提交前 790 项 → 提交后 **797 项**（新增 7 项回归，见各提交说明；
> 另含此前 41686fe 新增的 0 单测——拖动链路由 CDP 冒烟脚本覆盖）。
> 验证：`npm test` 797/797 全绿 · `npm run build` 通过 · 全路由 CDP 冒烟 43/43
> 零异常 · 悬浮按钮拖动冒烟（touch+mouse 双环境）6/6。

---

## 一、修复清单（审计项 → 提交 → 改动文件 → 测试证据）

| 审计项 | 修复内容 | 提交 | 改动文件 | 测试证据 |
|---|---|---|---|---|
| S1 | CSV/TSV/Anki 公式注入中和（`= + - @` 开头前置单引号；sheetCellGuard 三处共用） | `8e8f350` | exporters.js · sync.js | p0-fixes：`sheetCellGuard` 中和/白名单/toCSV 整链路（4 断言） |
| M1 | 三处时间戳漏 bump：applyCardFeedback→reviewedAt；recordQuickCheck→updatedAt；rescueCard/rescueAll→reviewedAt（不碰内容侧） | `8e8f350` | repo.js · quickCheck.js · Cards.vue | p0-fixes：livenessTs 推进 + updatedAt 原样（2 断言）；rescue 同款模式由全路由冒烟兜底 |
| R4 | Review.rate() 重入锁（ratingBusy + finally）；CardModal 已有 saving 锁、saveSmart 为同步短操作（现状免疫，以代码为准） | `8e8f350` | Review.vue | 键盘连按路径由 CDP 冒烟回归覆盖 |
| R2 | main.js 模块求值期 window 'error' 守卫（createApp 前）；App.vue 全局组件（FloatAssistant/NotificationBell/PlanReminderLayer/Intro/Guide）各自包 ErrorBoundary | `8e8f350` | main.js · App.vue | 全路由 CDP 冒烟 43/43（含白屏探测） |
| R3 | db.js blocked/versionchange 监听 + 双实例显式 open().catch → getDbStatus()/onDbStatusChange()；App.vue 顶部红色横幅消费 | `8e8f350` | db.js · App.vue | p0-fixes：状态机可订阅退订（1 断言） |
| B1 | registerType autoUpdate → prompt（编辑中不再被无条件 reload 丢内容；onNeedRefresh/「新版本」横幅由死代码变活） | `8e8f350` | vite.config.js | devOptions.enabled 保持 false；冒烟验证横幅链路可达 |
| M3 | hub 数据文件损坏不再静默重置：改名 `<file>.corrupt-<ts>` 留证 + 日志，再空数据继续 | `95a90a9` | sync-hub/hub.js | hub-safety：损坏改名留证 + 服务可继续写入 |
| M4 | hub PUT /backup 校验 version（不符/缺失 → 400 提示升级，不再静默丢新表返 200） | `95a90a9` | sync-hub/hub.js | hub-safety：版本不符/缺失 400、正确版本 200 |
| M7 | hub GET /backup 默认不下发隐私表，`?includePrivacy=1` 才下发 | `95a90a9` | sync-hub/hub.js | hub-safety：默认空数组 / 带参下发 |
| M2 | （复核已修：hub.js stale 清理 + 前端同口径——历史提交闭环，未重复改） | — | — | hub.js:179-189 现存 stale 收集过滤 |
| M5 | deleteCard 级联补全：notes.linkedCardIds 剔除 + 孤儿图片写 kind='image' 墓碑（reviews/embeddings/graphEdges/卡组关联/墓碑主体由 round15/16 已修） | `95a90a9` | repo.js | deletecard-cascade：笔记引用剔除/孤儿图墓碑/共享图不误删/无图零墓碑（2 断言×4 点） |
| M14 | privacyRecords 落盘加密判定与同步语义冲突 → 按审计最小方案落地：`.sxybrick` 加密备份导出/导入（AES-GCM+PBKDF2），crypto.js 由 0 调用死代码变真实被调用 | `580b1fc` | sync.js · Export.vue · Sync.vue · i18n | encrypted-backup：密文无明文/往返恢复/错口令与损坏文件报错（2 断言） |
| M10 | embeddings 兜底全表载入加 FULLSCAN_ROW_LIMIT=3000 上限 + 超限显式报错；semantic_search/retrieve_context 工具把错误转为可操作反馈 | `e64d063` | retrieval.js · agent/tools/index.js | 检索工具行为由全路由冒烟覆盖；上限逻辑见注释说明 |
| M11 | listCards 单次全表扫描（dueCount 复用同批数据，旧实现 2 次） | `e64d063` | repo.js | 全路由冒烟 /cards 页正常 |
| M12 | importBackup / previewImport 逐条比对改「键数+水位」快速淘汰 + 罕见兜底 stringify | `e64d063` | sync.js | 全路由冒烟（导出/同步页） |
| E3 | Cards.vue page 视图真分页（50/页 + 页码 + 越界收回）；scroll 视图 VirtualList 不动 | `e64d063` | Cards.vue | 全路由冒烟 /cards |
| E4 | ForgettingCurve resize 监听移入生命周期 + 卸载移除/dispose/置 null | `e64d063` | ForgettingCurve.vue | 全路由冒烟（含图表页） |
| M9 | LibraryFiles：图片 objectURL revoke + pdf 文档 destroy（切换/关闭/卸载三处） | `e64d063` | LibraryFiles.vue | 全路由冒烟 /materials |
| PWA manifest | lang zh-CN + apple-touch-icon（相对路径随 base 部署） | `e64d063` | vite.config.js · index.html | build 产物 manifest/HTML 检查 |
| 图片懒加载 | MarkdownRenderer md-img / Cards 孤儿图网格 loading="lazy" | `e64d063` | MarkdownRenderer.vue · Cards.vue | 全路由冒烟 |
| Dashboard 首屏 | loading 态 + 加载失败错误提示与「重试」（IndexedDB 故障不白屏不静默） | `e64d063` | Dashboard.vue | 全路由冒烟 /（首页） |
| 用户文案 | 「恐怖级埋点/B档详尽版/opt-in/PWA」中文化；错题计数统一「答错 N 次」；「电脑端地址」统一「同步中枢」 | `e64d063` | App.vue · i18n/views/{privacyData,dashboard,cards,sync}.js | i18n 闸门 zh/en 键位与占位符校验通过 |
| （前置任务）悬浮按钮拖动 | AI 对话球 60fps 跟手（touch-action/rAF/合成层/零 Vue 重渲染）+ 三处统一 useFabDrag | `41686fe` | useFabDrag.js · App.vue · FloatAssistant.vue · NotificationBell.vue | smoke-fab-drag 6/6（touch+mouse） |

---

## 二、复核后撤回 / 免修项（与提示词描述不符，以代码为准）

1. **M1-classify 撤回**（历史审计已确认）：classifier 不写 cards 表，无"改 subject 不 bump"问题。
2. **M5 主体**：deleteCard 级联（trash/墓碑/reviews/embeddings/graphEdges/cardGroupLinks）已由 round15/16 修复，本次只补笔记引用 + 图片墓碑两处残余。
3. **R1 ErrorBoundary**：去 async 修复已在历史提交落地，现状正确。
4. **R4 同类**：CardModal 已有 `saving` 锁 + 按钮 disabled + finally 复位；Cards.saveSmart 为同步短操作（prompt→push→persist），无重入窗口。
5. **M2**：hub stale 清理已存在（非 card 表 applyTombstones 的 stale 收集 + 过滤）。

## 三、判定不可行 / 未做项（DoD 第 5 条）

| 项 | 判定 | 原因与替代方案 |
|---|---|---|
| M14 落盘加密（完整接线） | 不可行 | privacyRecords 同时是「隐私同步 opt-in」载体：设备密钥各机不同 → 对端密文不可解，opt-in 同步名存实亡；共享口令派生密钥需新增隐私口令 UX，超"修复不改功能"边界。**已落地审计最小方案**：`.sxybrick` 加密备份（`580b1fc`）。 |
| Element Plus 按需引入 | 未做 | 架构级重构：动构建拓扑、触及 icons-vue 与 element-plus 同 chunk 白屏铁律，审计路线图亦排"架构级/下迭代"。本阶段以 E3 真分页先行消除最大渲染面。 |
| 阶段四 P3（M8 逻辑时钟 / M13 插件沙箱 / 测试补缺口 / OCR 真离线） | 未做 | 原始任务约定"仅当阶段一~三全部通过且用户明确确认后执行"；本会话未获确认。 |
| 文案清理遗漏项 | 已知 | i18n/index.js 中学习引擎帮助文案的 "opt-in"（文档性质）未改；Router/控制台等内部提示按"只改面向用户字符串"原则保留。 |

## 四、遗留风险提示

- hub 的 PUT version 校验会拒绝**旧客户端**（包版本 < 当前 BACKUP_VERSION）——与"旧 hub 收新包"对称的收紧，属预期；若历史部署过旧版本前端需同步升级。
- 加密备份入口提示"要包含隐私记录请先开启隐私同步"（沿用 buildBackup 的现有有效表语义）。
- 本次每次改动 repo/视图都会让 i18n 行号基线告警（"新增=消除"位移误报），处理方式为 `--update-baseline` 认领；建议后续 commit 拆分更小或让基线校验改为内容感知。
