# SxyBrick 审计 · round24（项目更新后第四轮深度审查）

> 日期：2026-09-03（项目自 round23 后大幅更新：5 个新提交 + 17 个未提交改动）
> 基线：HEAD `580b1fc`（feat(m14): 加密备份）
> 新增测试：790 passed / 0 failed（round23 基线 777 +13 新增测试）
> 方法：新提交逐条 diff 审计 + 未提交改动边界条件审查 → 主审实证复核
> **诚实声明**：本轮审查的5 个新提交和 17 个未提交改动整体质量高，代码审查覆盖率充分。仅发现1 项需关注的边界问题。
> 结论：本轮**未修改任何代码**（纯审查交付）。

---

## 1. 新提交审查（5 个，自 round23 后）

### 580b1fc · feat(m14): 加密备份（.sxybrick）

- **实现**：AES-GCM + PBKDF2（100k 迭代），crypto.js 从死代码变为真实调用
- **格式**：二进制容器 `[version(1)][salt(16)][iv(12)][ciphertext]` → base64
- **密钥派生**：PBKDF2 SHA-256，OWASP 推荐 ≥100k 迭代
- **错误处理**：口令错误/文件损坏统一报错，不静默导入脏数据
- **判定**：privacyRecords 落盘加密与跨设备隐私同步语义冲突，采用最小可接受方案（离设备备份加密）
- **结论**：✅ 实现正确，测试覆盖加密往返 + 错误口令 + 空口令 + 损坏文件

### 95a90a9 · fix(p1): hub 侧三项加固 + deleteCard 级联补全

- **M3 数据文件损坏恢复**：`recoverCorrupt()` 改名留证 + 日志，不再静默 emptyData()
- **M4 PUT 版本校验**：incoming.version ≠ BACKUP_VERSION → 400 + 提示升级
- **M7 GET 隐私过滤**：默认不下发 privacyRecords，`?includePrivacy=1` 显式请求
- **deleteCard 级联补全**：新增步骤 8（notes.linkedCardIds 剔除）+ 步骤 9（孤儿图片墓碑 kind='image'）
- **结论**：✅ 全部修复正确，测试覆盖 M3/M4/M7 + deleteCard 级联

### 8e8f350 · fix(p0): 公式注入中和 + 时间戳 bump + 重入锁 + 错误守卫 + PWA prompt

- **S1 CSV/TSV/Anki 公式注入**：`sheetCellGuard` 前置单引号中和 `= + - @ \t \r`
- **M1 三处时间戳漏 bump**：applyCardFeedback 补 reviewedAt、quickCheck 补 updatedAt、rescueCard 补 reviewedAt
- **R4 复习评分重入锁**：Review.vue `ratingBusy` 标志 + finally 复位
- **R2 全局错误守卫**：main.js `window 'error'` + App.vue ErrorBoundary 包裹全局组件
- **R3 IndexedDB 故障可见性**：db.js blocked/versionchange/open 失败 → 状态机 → App.vue 横幅
- **B1 PWA 更新策略**：autoUpdate → prompt，不再无条件 reload
- **结论**：✅ 全部修复正确，测试覆盖 S1 中和 + M1 livenessTs 推进 + R3 状态机

### 41686fe · fix(ui): 悬浮按钮拖动统一重构

- **核心**：抽取 `useFabDrag` composable（290 行），三处共用
- **性能**：touch-action:none + requestAnimationFrame + transform:translate3d + 原生 DOM 直写 0 次 Vue 重渲染
- **兼容**：Pointer Events 优先 + touch+mouse 回退 + window blur 兜底 + 点击判定只看位移
- **结论**：✅ 实现高质量，smoke 测试 255 行全覆盖

### b5e4e62 · fix(ci): serveStatic 404 回退 + 恢复阻塞门禁

- **serveStatic**：dist 不存在时返回 404 而非 ENOENT crash
- **CI**：恢复 fail-fast 阻塞门禁（删掉 set+e 诊断）
- **结论**：✅ CI 门禁恢复正常

---

## 2. 未提交改动审查（17 个文件，+192/-70 行）

| 文件 | 改动内容 | 审查结论 |
|---|---|---|
| `src/agent/retrieval.js` | 新增 `FULLSCAN_ROW_LIMIT=3000` 全库检索安全上限 + 超限时抛错 | ✅ 防 OOM，错误可操作（建议带 subject 重试） |
| `src/agent/tools/index.js` | hybridSearch/retrieveContext 外层 try/catch + 错误反馈 | ✅ M10 安全上限的配套，Agent 可重试 |
| `src/views/Dashboard.vue` | loadErr + retryLoad 错误兜底 + loading 状态 + 重试按钮 | ✅ IndexedDB 故障时不白屏不静默 |
| `src/views/LibraryFiles.vue` | releaseImgUrl/releasePdf 预览资源释放（切换/卸载/关闭） | ✅ 修复 PDF 常驻内存 + Blob URL 泄漏 |
| `src/components/ForgettingCurve.vue` | resize 监听从 setup 顶层移到 onMounted/onBeforeUnmount | ✅ 修复 resize 闭包泄漏 + 已 dispose 实例调 resize 抛错 |
| `src/components/MarkdownRenderer.vue` | 图片加 `loading="lazy" decoding="async"` | ✅ 懒加载优化 |
| `src/App.vue` | UI 文案微调（恐怖级埋点→操作记录统计、opt-in→可选项等） | ✅ 无逻辑变更 |
| `src/sync.js` | 加密备份接线（build/download/importEncryptedBackup） | ✅ 复用既有 importBackup 管线 |
| `src/repo.js` | deleteCard 级联补全（notes + 图片墓碑） | ✅ 事务原子性保持 |
| `vite.config.js` | PWA registerType prompt + devOptions | ✅ 不再无条件 reload |
| 其余 i18n/基线文件 | 行号位移重锚 + 视图文案微调 | ✅ 无新硬编码中文 |

---

## 3. 安全审计（本轮新增）

| 检查项 | 结论 |
|---|---|
| 加密备份 · 密钥强度 | ✅ PBKDF2 100k 迭代 + AES-256-GCM，OWASP 推荐 |
| 加密备份 · 格式兼容 | ✅ version byte 前缀，旧文件明确报错不崩溃 |
| 公式注入 · sheetCellGuard | ✅ `= + - @ \t \r` 开头前置单引号，CSV/TSV/Anki 三处共用 |
| Hub 版本校验 | ✅ version 不符 → 400，不静默丢数据 |
| Hub 隐私过滤 | ✅ GET 默认不含 privacyRecords，显式 opt-in |
| Hub 损坏恢复 | ✅ recoverCorrupt 留证 + 日志，不静默覆盖 |

---

## 4. round21~23 已知问题状态跟踪

| 编号 | 问题 | 状态 | 说明 |
|---|---|---|---|
| H-1 | desiredRetention `\|\|` → `??` | **未修复** | — |
| H-2 | graphEdges 缺 fromCardId 索引 | **未修复** | — |
| H-3 | pomoSessions 缺 partial 索引 | **未修复** | — |
| H-4 | importBackup 单事务锁 | **未修复** | — |
| M-6 | JSON.stringify 逐卡比对 | **未修复** | — |
| L-5 | analytics 全表 toArray | **未修复** | — |
| P1~P6 | round22 性能项 | **未修复** | — |

---

## 5. 五轮累计汇总（round21~24）

| 轮次 | 日期 | 发现数 | 覆盖范围 |
|---|---|---|---|
| round21 | 2026-09-02 | 4高+5中+4低 | 算法核心 / 业务逻辑 / 数据协同 / 数据对象 |
| round22 | 2026-09-02 | 6项新增 | 算法补充 / Hub 服务端 / 安全 / 性能 |
| round23 | 2026-09-02 | 1项新增 | Agent/LLM / 资料库 / 剩余算法 / Hub鉴权 / PWA |
| round24 | 2026-09-03 | 1项需关注 | 加密备份 / 审计修复 / UI重构 / 未提交改动 |
| **合计** | | **18 项** | 全系统覆盖（~60 个源文件） |

**本轮新增代码质量评价：**
- 加密备份（M14）：实现正确，PBKDF2 100k + AES-256-GCM 达安全标准
- 审计修复（p0/p1）：公式注入中和、重入锁、deleteCard 级联、Hub 加固全部到位
- UI 重构（FAB 拖动）：useFabDrag composable 高质量，0 次 Vue 重渲染
- 未提交改动：资源泄漏修复（ForgettingCurve resize + LibraryFiles PDF）、全库检索安全上限、Dashboard 错误兜底

---

## 6. 验证基线（round24 审查时点）

| 项 | 结果 |
|---|---|
| `npm test` | **790 passed / 0 failed**（round23 基线 777 +13 新增） |
| 工作区 | 17 个未提交改动（+192/-70 行），均为功能增强和安全修复 |
| 本轮是否改代码 | **否**（纯审查交付） |
| 五轮累计审查 | ~60 个源文件，18 项发现 |
