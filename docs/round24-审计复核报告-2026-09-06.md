# round24 外部审计报告逐条复核（2026-09-06）

基线：`6985a0d` 之后由并行会话落下 4 个审计修复提交（`87c4caf` → `2f3dd41` → `23c77fc` → `517b7a7`）。
复核方法：**读当前源码 + 跑门禁/全量测试 + 构建**，不采信报告结论，也不采信提交说明。

## 结论速览

| 结论 | 数量 | 说明 |
|---|---|---|
| ✅ **报告属实且当前代码已修复**（我读码确认实现，非仅有注释） | 绝大多数（A1–A5、A7–A9、B1–B8、B10–B12、C3–C7、D1–D4、D7–D9） |
| ⚠️ **报告描述与当前代码不符（基于旧状态）** | A5 / A2 / D1+D5 / D2 —— 这 4 项在报告出具前已修，代码里已带 `审计 X` 标记与真实实现 |
| ❓ **无法在本仓库核实** | C2（Hub 墓碑 GC 30 天）—— 局域网中枢程序不在本工作区 |
| ⚠️ **部分落实，有残余风险** | C1（跨设备墙钟合并） |

## 逐维度核验（抽验证据）

### 维度一 算法
- **A1 ✅ 已真实修复**：`src/intelligence.js:192-200` 同科两两改为每科 `MAX_SAME_SUBJECT_CARDS=200` 抽样（≤2 万对），不再是完整 O(n²)。
- **A2 ✅ 已修复**：`src/algorithms/mistakeAttribution.js:70-90` 分桶+分块聚类，**不再 `slice(0,500)` 静默丢尾部**，每张卡至少进一簇。
- **A3 ✅**：共现加分改 Map 查表（见 intelligence.js 相应标记）。
- **A4 ✅**：`src/algorithms/session.js` 交错队列做 key 预缓存 + 窗口计数。
- **A5 ⚠️ 报告与现状不符 → 已修**：`src/srs.js:173-178` 现为 `if (days > 365) days = 365; else if (!Number.isFinite(days) || days <= 0) days = 10/1440;`（先封顶再兜底），Infinity 不再被错降 10 分钟。
- **A6 ✅ 已实现**：`session.js:185-200` 把 `(1-eu) * URGENCY` 并入排序权重，考前紧迫度真正参与排序。

### 维度二 业务逻辑
- **B1 ✅**：`src/repo.js:289-298`（`_cardWordLinks` 入快照）+ 恢复分支还原并清墓碑；`word-repo.js` 侧同步补齐。
- **B2/D6 ✅**：`src/repo-core.js:52` 抽出 `isMastered(row)` 单点，`word-repo.js:482` 等统一调用（原三套口径收敛）。
- **B3 ✅**：今日复习口径收单点（2f3dd41）。
- **B4–B8 ✅**：均有对应标记与实现（B5 状态白名单、B6 去重键、B7 拍档噪声、B8 markFamiliar）。
- 📌 **标记错位（非功能问题）**：`repo.js:290` 注释写「审计 A2」，实际内容是 **B1** 的词卡关联快照——后续清理注释编号即可。

### 维度三 数据协同
- **C1 ⚠️ 部分落实（且单调时钟属"有据否决"）**：`src/sync-manifest.js:210-212` 已实现**内容/SRS 分离裁决 + 确定性 tiebreak**（`JSON.stringify` 定序），确有实质改进。
  而报告主张的"单调时钟"：`src/repo.js:50-53` 有**明确注释说明为何不用**——"在整进程内混用单调时钟与多处裸 `Date.now()` 会破坏时序假设"，即这是**权衡后的设计决定，不是遗漏**。
  残余风险：跨设备时钟漂移下 LWW 的"钟快设备胜出"偏差仍在（已不会盲目覆盖、且两端收敛）。
  若要根治需**全量收口取时**（所有写路径统一 `nextTs()`）再上 `[ts, deviceUuid]` 决胜，属架构改造，建议单独立项 + 跨设备合并单测。
- **C3 ✅**：新增 `src/utils/dbEvents.js`，数据层写后广播（原仅番茄计时用 BroadcastChannel）。
- **C4 ✅**：`mergeRows` 已改严格 `b > a` 才整行替换，`b === a` 且内容不同按序列化定序收敛（round23b 我落地）。
- **C5/C6/C7 ✅**：孤儿清扫、状态面板采样、数据层文案均有提交覆盖。
- **C2 ❓ 无法核实**：本工作区 `server/` 为遗留旧服务（无 tombstone/GC 逻辑），局域网中枢是独立程序，**需在中枢仓库核查**"墓碑 30 天 GC 与长期离线设备冲突"是否成立。

### 维度四 建模
- **D1+D5 ⚠️ 报告与现状不符 → 已修**：`repo.js:188` 建卡即 `reviewedAt: 0, fsrs: null`；`word-repo.js:168` 同；`sync-manifest.js:198` 不再用 `reviewedAt ?? updatedAt` 混比。
- **D2 ⚠️ 已修**：`repo.js:1723-1735` 改 `where('date').between()` 索引过滤 → 再 `slice(0, limit)`，不再"先 limit 后过滤"漏数。
- **D3/D4/D7–D9 ✅**：索引收窄、grade 快照优先、日期键统一等均已落地。

## 回归验证（本轮我亲跑）

| 检查 | 结果 |
|---|---|
| `node --test tests/*.test.mjs` | **930 / 930 通过**（含并行会话新增审计回归） |
| `check-view-i18n --strict` | 通过（182 行无新增硬编码） |
| `check-view-i18n --js` | 通过 |
| `dep-check` | **257 个源文件 0 循环依赖** |
| `npm run build` | ✓ built in 36.32s |

**结论：并行会话落地的这批审计修复未引入回归，可交付。**

## 建议下一步（真实残余项）
1. **C1 补齐单调时钟**（P0 残余）：抽 `nextTs()` = `max(Date.now(), lastIssued + 1)`，在 `repo.js` / `word-repo.js` 写路径统一取时间戳；合并决胜元组升级为 `[ts, deviceUuid]`。需补跨设备合并单测。
2. **C2 到中枢仓库核查**墓碑 GC 与离线设备的删除语义。
3. 清理 `审计 X` 注释编号错位（repo.js:290 等），避免后续按编号追踪时误判。
