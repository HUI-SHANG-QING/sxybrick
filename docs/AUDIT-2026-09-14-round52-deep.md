# SxyBrick 深度代码审计报告 — Round 52

> **审计日期**：2026-09-14
> **审计基线**：git HEAD `c90e809`（round51 后唯一新提交：LLM 最大输出长度开放用户配置）
> **本轮性质**：事件驱动审计（round51 建议）——新代码面深审 + **顺手修掉 round50 遗留的 2 个 P3**
> **测试状态**：全量 **1093/1093 通过**（较 round51 的 1088 新增 5 个：round52 回归 3 个 + c90e809 自带 2 个）
> **上轮报告**：`docs/AUDIT-2026-09-14-round51-deep.md`

---

## 一、总评（大白话）

这轮干了两件事：审掉新迭代的一个提交（AI 输出长度配置），顺手把 round50 挖出的两个"显示层小瑕疵"修掉了。**新代码面无缺陷，round50 待办清零。**

- **新提交 `c90e809`（LLM 输出长度开放配置）**：审过，写得干净。取值优先级链清晰（调用方显式指定 > 用户在设置里选的 > 默认 8192），非法值有 `Number.isFinite` 防线；旧配置没有这个字段时回落默认（还特别防了展开运算符带进 undefined 的坑）；用户设了超出模型上限的值也不打死请求——服务端提示后自动降级重试一次。自带 2 条测试。
- **round50 N1（输入长度上限）**：修复中发现它**一半是误报**——卡片侧的长度上限其实早就有（`validateCard` 的 8000 字上限，还是码点安全写法），真正缺的只有词卡侧。本轮补上：词卡文本字段软截断（word 200 / meaning·example·note 5000 / source 60 / subject 30）。
- **round50 N2（emoji 截断）**：AI 上下文出口的截断从"按半个字符切"改成"按完整字符切"——emoji 骑在截断线上不再被劈成乱码。顺带把截断函数导出，回归测试可以直接测它。

---

## 二、本轮修复详情

| 编号 | 修复 | 位置 | 验证 |
|---|---|---|---|
| round50 N1 | 词卡文本字段写入边界软截断（`L(s, max)`：word 200 / meaning·example·exampleTrans·note 5000 / source 60 / subject 30）；卡片侧核实为**已有防线**（`MAX_CHARS=8000`，`ee8b39c` 引入），本轮只补边界断言 | `word-repo.js:183-191` | 回归 3/3 ✅ |
| round50 N2 | AI 上下文截断改码点安全（`Array.from(s).slice(0,1500).join('')`）；`compactConvo` 导出供回归直测 | `agent/agents/base.js:119-131` | 回归 3/3 ✅ |

**回归测试**（`tests/round50-n1-n2.test.mjs`，3 条）：
1. 词卡五字段超长输入 → 静默截断到各自上限
2. 卡片侧超 8000 字拒绝 + emoji 按码点计长不误伤（7999 ASCII + 1 emoji 放行）
3. 30000 个 emoji（超 48000 上下文预算）触发截断 → 输出恰为 1500 个**完整** emoji，无半个代理对

**过程中处理**：base.js 加注释致行号位移，i18n 数据层闸门按 file:line 基线误报 5 处"新增中文"——核实为行号漂移（存量行，diff 仅注释 +3），用 `--js-update-baseline` 重写基线认领（437 行不变），三道闸复检通过。

---

## 三、历轮修复完整性验证（用户点名要求）

### round43–48 修复标记存活复验（8/8 健在）

| 轮次 | 标记位置 | 状态 |
|---|---|---|
| round43 N1 空轨迹守卫 | `fsrs.js:273` | ✅ |
| round43 N2 IMAGE_REF_TABLES | `images.js` | ✅ |
| round43 N3 sweep 墓碑 | `repo.js` | ✅ |
| round44 N1 dueOf 统一口径 | `repo-core.js` | ✅ |
| round44 N2 围栏剥离 | `utils/ai-structured.js` | ✅ |
| round45 N1 chat 合并确定性 | `sync-manifest.js` | ✅ |
| round48 记忆修剪/墓碑 | `agent/memory.js`（2 处） | ✅ |
| round48 错误分类 | `utils/offlineAI.js` | ✅ |

**十二轮审计（43→52）累计修复 32 项**（30 历史 + 本轮 2），零遗失、零回退。round50 的 2 个 P3 待办全部清零——当前**无任何未修的已知 P0/P1/P2/P3**。

---

## 四、本轮新发现

**无新发现**——新代码面（`c90e809`）审查通过，历史待办已清零。

---

## 五、审计结论（大白话）

1. **修复清单首次见底**：十二轮审计挖出的 32 个问题全部修完，known-issues 队列归零。
2. **round50 N1 的"半误报"是个好教训**：当时说"所有入库路径都没有长度校验"，这轮实际去修才发现卡片侧早就有（8000 字上限）——审计报告的"全覆盖"结论也要带着怀疑验证，动手修时如实修正认知，这也是为什么坚持"每个结论要有行号实证"。
3. **审计模式正式转事件驱动**（round51 建议、本轮执行）：新迭代面小就顺手清待办，不再产出全文报告周期空转。下次全文审计触发条件：功能性大版本迭代、新依赖引入、或用户报障。

---

*审计完成时间：2026-09-14 | 基线：c90e809 | 测试：1093/1093 pass · i18n 三道闸通过 | 历史修复复验：8/8 健在 | 待办清零*
