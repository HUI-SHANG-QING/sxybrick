# round26 深度审查报告（2026-09-07，含移动端/平板 UI 专项）

方法：三路并行只读审查（移动端 UI / 算法·性能 / 数据协同·建模）+ 主审读码复核关键项。
重点为「看不见的底层问题」+ 用户点名的移动端 UI。★ = 本轮已修复；✎ = 待办。

## 一、移动端 / 平板 UI 兼容（重点）

| # | 严重度 | 位置 | 问题 / 根因 / 建议 | 状态 |
|---|---|---|---|---|
| M1 | P1 | `AIAssistant.vue:439-443, 510-513` | **AI 助手手机端"反人类"根因**：≤720px 媒体查询只把父网格改成 1 列，但 4 个子元素仍带 `grid-column:2/3` 显式定位 → 浏览器生成隐式列，消息流 `.chat-box` 被塞进按内容收缩的窄列，只剩细条。**已修**：媒体查询内显式重置为纵向堆叠（历史→全屏行→消息流占满→轨迹），输入区窄屏换行、气泡 max-width 92% | ★ |
| M2 | P1 | `element-bridge.css`（新增兜底）；`UncloseableModal 520px` / `WordGroups 560px` 等 | el-dialog 内联固定 px 宽在 375px 视口溢出、按钮不可点。**已修**：≤768px 全局 `.el-dialog{width:92vw!important}` + body 限高滚动 | ★ |
| M3 | P2 | `AIAssistant.vue:434` 等 | `100vh` 未用 `dvh`，移动浏览器地址栏收起时底部被遮挡。**已修 AI 助手**（`calc(100dvh-140px)`，vh 回退）；App.vue/AgentWorkbench/Mindmap 待统一 | ★(部分)/✎ |
| M4 | P2 | `WordReview.vue:...` 学习页 | 学习页为移动主场景之一，需抽查：题目区在 320-375px 的换行/字号、评级按钮 4 档触达尺寸 | ✎ |
| M5 | P2 | `FullscreenButton 28px` / `ChartZoomBar 24px` | 触摸目标 <40px，且全局 `coarse 指针 min-height:44px` 规则未覆盖这两类 | ✎ |
| M6 | P3 | `App.vue` 全局 | 无 `safe-area-inset-bottom`、无 `-webkit-tap-highlight-color` 禁用；建议统一补 | ✎ |

## 二、算法与性能

| # | 严重度 | 位置 | 问题 / 根因 / 建议 | 状态 |
|---|---|---|---|---|
| A1 | P2 | `networth.js:22-53` + `word-repo.js:92` | FSRS→SM-2 切换后 `fsrs.s/d` 冻结仅 bump `last`，净值 `R≈1` 高估。建议 retentionOf 按 active scheduler 分流、切回 SM-2 清空 fsrs | ✎ |
| A2 | P2 | `scheduling.js:50-60` | `examWindowUrgency` 用 `fsrs?.s ?? 1` 无有限性守卫，`s=NaN` → rank=NaN → 考前队列静默沉底。加 `Number.isFinite` 守卫 | ✎ |
| A3 | P2 | `word-repo.js:393/471/499` | `listWordCards/wordStats/wordGroupStats` 全表 `toArray()`；`db.js` 已有 `kind/familiar/dueAt` 索引未用。大词库主线程卡顿 | ✎ |
| A4 | P3 | `repo.js` 统计 / `achievements.js:37` | `reviews`（无界最大表）每次全量 `toArray()` 重算、无 TTL 缓存 | ✎ |
| A5 | P3 | `fsrs.js:259-262` | `trainWeights` 的 `reps===0` 分支覆盖传入 s/d，语义误导（存量卡重训缺"续推"路径） | ✎ |
| A6 | P3 | `intelligence.js:324` | `catch{}` 静默吞异常，共现分析回归不可观测 | ✎ |
| A7 | P3 | `repo-core.js:52` vs `networth.js:100` | "已掌握"仍两套口径（level/interval vs R≥0.9） | ✎ |

## 三、数据协同与建模

| # | 严重度 | 位置 | 问题 / 根因 / 建议 | 状态 |
|---|---|---|---|---|
| D1 | P2 | `sync-manifest.js:18-28` + `db.js` | **imageRefs(v32) 漏登记** → 审计"1 漏同步"、破坏「清单=唯一事实源」不变量。**已修**：EXCLUDED_FROM_SYNC 补 `imageRefs`，审计恢复「所有表均已纳入同步或被显式排除」 | ★ |
| D2 | P2 | `repo.js` 写路径 vs `utils/dbEvents.js` | 跨 tab 广播只在 importBackup 后调用，常规 CRUD 写路径未接 → 双 tab 编辑互相覆盖（dbEvents 仅通知、无版本守卫） | ✎ |
| D3 | P2 | `repo.js:457-459` / `word-repo.js:235-238` | 卡片删除后**图片墓碑在事务外补写**：若崩溃，图已删而墓碑未写 → 删除不跨设备传播 | ✎ |
| D4 | P2 | `repo.js:472-527` | `imageRefs` 索引快速路径写入从不维护（无调用方），`cleanupOrphanImages` 走索引会误删仍被引用的图；当前因 indexedCount=0 走全扫描兜底属潜伏陷阱 | ✎ |
| D5 | P2 | `sync.js:644-705` | importBackup 在大事务内对全部同步表（含 userOps/embeddings 十万级）`toArray()`+stringify 比对，长事务高内存，极端卡死 | ✎ |
| D6 | P3 | `repo.js:374` | restoreFromTrash 事务内 `await import()` 非表操作（当前在事务尾部风险低，反模式） | ✎ |
| D7 | P3 | tombstones | 墓碑无 GC/体积上限，随使用膨胀（无活 bug，长期健康项） | ✎ |

## 四、验证与交付
- ★ 本轮修复：M1（AI 助手移动端网格+输入区）、M2（el-dialog 小屏兜底）、M3（AI 助手 dvh）、D1（imageRefs 登记）。
- 验证：`node --test` **930/930**；i18n 双闸（基线重锚 3 处插行位移误报）；dep-check **258 文件 0 环**；`sync-coverage` ✓ 全表登记；build ✓ 37.7s。

## 五、建议后续顺序
1. M4/M5 移动端剩余（学习页触达尺寸、触摸目标）——用户体感项
2. D2 跨 tab 写路径广播 + 乐观并发守卫（多设备/多 tab 数据安全）
3. A3 word 索引收窄（规模到达前）
4. A1 networth 按调度器分流（数值面板正确性）
5. D3/D4 图片墓碑并入事务 + imageRefs 索引路径决策
