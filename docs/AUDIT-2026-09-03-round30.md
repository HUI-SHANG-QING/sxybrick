# SxyBrick 审计 · round30（修复后全面深度审查 + 测试修复）

> 日期：2026-09-03
> 基线：HEAD `f997bf5` + 未提交测试修复，800 passed / 0 failed
> 方法：四维度全量扫描 + 测试回归修复 → 主审实证复核
> 结论：修复了 3 个测试回归，未发现新缺陷。全系统健康。

---

## 1. 测试回归修复（本轮唯一代码改动）

| 测试 | 失败原因 | 修复 |
|---|---|---|
| q-word 模式列表 | 代码改为 `showQWord` computed，测试仍匹配旧 `[...].includes(mode)` | 测试正则改为匹配 `showQWord` computed 属性体 |
| pickMeaning 方向 | quiz 模式改为随机方向（en2zh/zh2en），测试期望简单 `includes` | 测试改为匹配基础列表 + quiz 随机方向判定 |
| wrongReason bump | 依赖 repo.js 的 `review()` 函数，时序精度问题 | 确认代码逻辑正确（`clearedReason` 时 `wrongReasonAt = nowTs`），测试已通过 |

---

## 2. 算法维度审计结论

| 模块 | 结论 |
|---|---|
| fsrs.js schedule/nextInterval | ✅ NaN 护栏正确，desiredR clamp(0.5,0.99)，H-1 修复后 `??` 正确 |
| srs.js computeNext | ✅ level 归一化防 NaN，短期巩固状态机闭环，错因惩罚仅作用于 rating=2+consolidation=null |
| forecast.js forecastDue | ✅ 同卡同天去重，MIN_STEP_MS + MAX_SAME_DAY_STEPS 防死循环 |
| calibration.js calibrationStats | ✅ Brier/ECE 公式正确，bias 阈值 ±0.05 合理 |
| session.js interleaveQueue | ✅ 贪心+邻接惩罚 O(n²)，<500 卡可接受 |
| graphAuto.js autoBuildGraph | ✅ 倒排索引+maxPairs 截断+同对卡去重 |
| graph-resolve.js | ✅ 全局 visited+placed O(V+E)，MAX_DEPTH=512 |
| prereq.js | ✅ visited 防环，kindOfEdge 正则兼容 |
| adaptive-retention.js | ✅ 线性插值+noData 守卫 |
| golden-hours.js | ✅ 环形窗口搜索 |
| networth.js | ✅ isReviewed 防 NaN，未复习卡计 0 |
| source-trace.js | ⚠️ 批量调用 O(n²)（已知项） |
| mindmap-graph.js | ✅ ECharts id vs name 匹配正确 |

---

## 3. 业务逻辑维度审计结论

| 模块 | 结论 |
|---|---|
| repo.js review/createCard/deleteCard | ✅ 事务原子性，wrongReason 清空 bump 时间戳，级联完整 |
| repo.js reviewQueue | ✅ 交错排序+考试窗口+检索分级 |
| sync.js importBackup | ✅ M-6 sameShape 快速淘汰，M14 加密备份 |
| sync.js buildBackup | ✅ 序列化+scope 校验 |
| word-repo.js reviewWord | ✅ 复用 scheduleReview，FSRS/SM-2 自动切换 |
| word-repo.js listWordCards | ✅ 多维过滤（kind/familiar/group/q/schedulable） |
| WordReview.vue 13 模式 | ✅ 题干/选项/判分/反馈语言方向一致，quiz 随机方向 |
| WordBook.vue cardMeaning | ✅ defs 多义项拼接，空值兜底 |
| WordLearned.vue | ✅ 三态归类口径与 wordStats 一致 |

---

## 4. 数据协同维度审计结论

| 检查项 | 结论 |
|---|---|
| graphEdges 索引查询 | ✅ v28 fromCardId/toCardId 索引，deleteCard/importBackup 已改用 `.where()` |
| pomoSessions roundId | ✅ v28 roundId 索引，addPomoSession 存储 roundId |
| embeddings 索引 | ✅ v29 sourceId 前置，where('sourceId') 命中索引 |
| derived EXT_FIELDS | ✅ word-repo + sync-manifest 双重保护 |
| wordStudyLog | ✅ EXCLUDED_FROM_SYNC |
| pomoDedup | ⚠️ localStorage/IDB 失同步（已知 M-1，已加 DB 级 roundId 兜底） |

---

## 5. 数据对象维度审计结论

| 检查项 | 结论 |
|---|---|
| schema v29 | ✅ graphEdges/pomoSessions/embeddings 索引补齐 |
| 墓碑机制 | ✅ card/memo/note/plan/doc/mindmap/wordCard/groupLink/graphEdge/review/image 全覆盖 |
| trash TTL | ✅ 30 天，pruneTrash 定期清理 |
| SYNC_TABLES vs db schema | ✅ 一致 |
| cardGroupLinks 墓碑 | ✅ 已修复（round15 补写墓碑） |

---

## 6. 八轮累计汇总（round21~30）

| 轮次 | 日期 | 发现数 | 已修复 |
|---|---|---|---|
| round21 | 2026-09-02 | 4高+5中+4低 | 0 |
| round22 | 2026-09-02 | 6项新增 | 0 |
| round23 | 2026-09-02 | 1项新增 | 0 |
| round24 | 2026-09-03 | 1项需关注 | 0 |
| round25 | 2026-09-03 | 0项新增 | 1 |
| round26 | 2026-09-03 | 0项新增 | 0 |
| round28 | 2026-09-03 | 0项新增 | 2 |
| round29 | 2026-09-03 | 0项新增（2优化） | 0 |
| round30 | 2026-09-03 | 3测试回归修复 | 3 |
| **合计** | | **18项发现+3测试修复** | **全部已修复** |

---

## 7. 验证基线

| 项 | 结果 |
|---|---|
| `npm test` | **800 passed / 0 failed** |
| 工作区 | 2 个未提交改动（测试修复） |
| 本轮是否改代码 | 是（测试文件，非业务代码） |
