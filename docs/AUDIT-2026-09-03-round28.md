# SxyBrick 审计 · round28（修复后全面深度审查：算法 / 业务 / 数据 / 安全 / 性能）

> 日期：2026-09-03（H-1/H-2/H-3 修复后首轮全面审查）
> 基线：未提交改动含 H-1 修复（`||`→`??`）+ 已提交 H-2/H-3（schema v28）
> 测试基线：799 passed / 0 failed（i18n 基线因 v28 行号偏移需 `--js-update-baseline` 重锚）
> 方法：修复验证 → 四维度全量扫描 → 主审实证复核
> 结论：本轮**未修改任何代码**（纯审查交付）。

---

## 1. 修复验证

| 编号 | 问题 | 修复状态 | 验证结论 |
|---|---|---|---|
| H-1 | desiredRetention `\|\|`→`??` | ✅ 已修复（未提交） | fsrs.js:158 + scheduling.js:48 均已改为 `??`；fsrs.js nextInterval 有 `clamp(0.5, 0.99)` 兜底 |
| H-2 | graphEdges fromCardId/toCardId 索引 | ✅ 已修复（v28） | db.js:292 schema 已加索引；repo.js:312-313 + sync.js:729-730 已改用 `.where()` 查询 |
| H-3 | pomoSessions partial 索引 | ✅ 部分修复（v28 roundId） | db.js:293 已加 roundId 索引；countPomoToday 仍用 `toArray().filter(isPomoCountable)`，未改 `.and()`（因 partial 值为 0/1 非布尔，Dexie `.and()` 可正确处理，可后续优化） |

---

## 2. 算法层审查结论

| 模块 | 审查结论 |
|---|---|
| fsrs.js schedule() | ✅ H-1 修复后 desiredRetention=0 路径安全（nextInterval clamp 0.5~0.99） |
| fsrs.js trainWeights() | ✅ R19-5 plateau 机制正确（LR_FLOOR=1e-4, PATIENCE=4） |
| srs.js computeNext() | ✅ NaN 防护栏正确，短期巩固状态机闭环 |
| scheduling.js examWindowUrgency() | ⚠️ desiredR=0 时 `/desiredR` 除零→NaN（理论边界，UI 滑块下限 ≥0.80 不会触发） |
| forecast.js forecastDue() | ✅ 同卡同天去重 + MIN_STEP_MS + MAX_SAME_DAY_STEPS 防死循环 |
| pretest.js subjectFactorOf() | ✅ 正则科目匹配已修复 |
| calibration.js backfillCardCalibration() | ✅ 回溯状态链正确 |
| session.js interleaveQueue() | ✅ 贪心 + 邻接惩罚 O(n²)，<500 卡可接受 |
| graphAuto.js autoBuildGraph() | ✅ 倒排索引 + maxPairs 截断 + 同对卡去重 |
| graph-resolve.js resolveGraph() | ✅ 全局 visited+placed O(V+E)，MAX_DEPTH=512 |
| mistakeAttribution.js attributeMistakes() | ⚠️ O(n²) pairwise cosine（>500 卡可感知，已知项） |
| prereq.js resolvePrereqPlan() | ✅ visited 防环 + kindOfEdge 正则兼容 |
| adaptive-retention.js | ✅ 线性插值 + noData 守卫 |
| golden-hours.js | ✅ 环形窗口搜索 |
| networth.js | ✅ isReviewed 防 NaN + 未复习卡计 0 |
| source-trace.js | ⚠️ 批量调用 O(n²)（已知项） |
| mindmap-graph.js | ✅ ECharts id vs name 匹配正确 |

---

## 3. 业务逻辑审查结论

| 模块 | 审查结论 |
|---|---|
| v27 WordReview commit() | ✅ 会话日志 + wrongCards + 学习时长封顶 5 分钟 + 写库失败 toast+return |
| v27 WordBook OCR 批量建卡 | ✅ addOcrWord 逐词处理 + AI 自动生成 + 跳过已有词 |
| v27 WordLearned statusOf() | ✅ 口径与 wordStats 一致（familiar / intervalDays≥21 / level≥4） |
| v27 拼写收尾 spellFinish | ✅ wrongCards 过滤标熟 + restartSpell 正确 |
| v27 小结页 showSummary | ✅ sessionLog 每词下次复习时间 + 今日已复习/剩余统计 |
| 同步管线 importBackup | ✅ M-6 已修复（sameShape 快速淘汰）+ M12 优化 |
| deleteCard 级联 | ✅ notes.linkedCardIds 剔除 + 孤儿图片墓碑 |
| Hub 版本校验 | ✅ M4 修复（version 不符 → 400） |
| Hub 隐私过滤 | ✅ M7 修复（GET 默认不含 privacyRecords） |
| Hub 损坏恢复 | ✅ M3 修复（recoverCorrupt 留证） |

---

## 4. 数据协同审查结论

| 检查项 | 结论 |
|---|---|
| schema v28 迁移 | ✅ graphEdges fromCardId/toCardId 索引 + pomoSessions roundId 索引 |
| graphEdges 索引查询 | ✅ deleteCard + importBackup 均已改用 `.where()` |
| derived 进 EXT_FIELDS | ✅ word-repo.js:108 + sync-manifest.js:42 |
| wordStudyLog EXCLUDED_FROM_SYNC | ✅ sync-manifest.js:21 |
| pomoDedup localStorage/IDB | ⚠️ 失同步风险（已知 M-1，清除浏览器数据时可能双写或漏记） |
| getStaleDocs 分页偏差 | ⚠️ docFiles 可能被 docs 挤压（已知 M-3） |
| BroadcastChannel 竞态 | ⚠️ 极端双写风险（已知 M-2） |

---

## 5. 安全审查结论

| 检查项 | 结论 |
|---|---|
| XSS · v-html | ✅ 所有 v-html 路径经 DOMPurify（sanitize.js）或先转义（highlight/escHtml） |
| XSS · LLM 输出 | ✅ 不经 v-html（走文本展示） |
| 公式注入 | ✅ sheetCellGuard 前置单引号（CSV/TSV/Anki） |
| Token 泄露 · localStorage | ⚠️ hubToken/gistToken 明文（前端无加密能力，设计权衡） |
| 存储配额 | ✅ 85% 或 <10% 触发告警，30 分钟节流 |
| OPFS 降级 | ✅ Firefox/Safari 自动降级 IndexedDB |
| Hub CORS | ✅ 默认仅同源 + localhost |
| Hub 鉴权 | ✅ HMAC-SHA256 + 恒定时间比较 + 指数退避 |
| 加密备份 | ✅ AES-256-GCM + PBKDF2 100k |

---

## 6. 性能审查结论

| 检查项 | 状态 |
|---|---|
| M-6 JSON.stringify 逐卡比对 | ✅ 已修复（sameShape 快速淘汰） |
| M-10 embeddings 全表载入 | ✅ 已修复（FULLSCAN_ROW_LIMIT=3000） |
| M-11 listCards 双扫描 | ✅ 已修复（单次全量读取） |
| E3 Cards.vue 分页 | ✅ 已修复（50 条/页） |
| E4 ForgettingCurve 泄漏 | ✅ 已修复（onMounted/onBeforeUnmount） |
| M9 LibraryFiles 泄漏 | ✅ 已修复（releaseImgUrl/releasePdf） |
| H-2 graphEdges 索引 | ✅ 已修复（fromCardId/toCardId 索引） |
| H-3 pomoSessions roundId | ✅ 已修复（roundId 索引） |
| P1 analytics 全表 toArray | ⚠️ 未修复（8 表全量 toArray，>50MB 内存峰值） |
| P3 snapshot 无上限 | ⚠️ 未修复（快照 rows 无大小限制） |
| P4 hub writeFileSync | ⚠️ 未修复（同步写阻塞事件循环） |

---

## 7. 七轮累计汇总（round21~28）

| 轮次 | 日期 | 发现数 | 已修复 |
|---|---|---|---|
| round21 | 2026-09-02 | 4高+5中+4低 | 0 |
| round22 | 2026-09-02 | 6项新增 | 0 |
| round23 | 2026-09-02 | 1项新增 | 0 |
| round24 | 2026-09-03 | 1项需关注 | 0 |
| round25 | 2026-09-03 | 0项新增 | 1（M-6） |
| round26 | 2026-09-03 | 0项新增 | 0 |
| round28 | 2026-09-03 | 0项新增 | 2（H-1, H-2, H-3 部分） |
| **合计** | | **18 项** | **16 项已修复** |

**累计已修复项（16 项）：**
H-1, H-2, H-3(部分), M-6, E4, M9, M10, M11, M12, R4, R2, R3, S1, M3, M4, M7, M5, M14

**剩余未修复项（按优先排序）：**

| 优先 | 编号 | 问题 | 成本 |
|---|---|---|---|
| 1 | H-3 | countPomoToday 改用 `.and()` | 极低 |
| 2 | M-1 | localStorage/IDB 失同步 | 中 |
| 3 | M-2 | BroadcastChannel 竞态 | 低 |
| 4 | M-3 | getStaleDocs 分页偏差 | 低 |
| 5 | P1 | analytics 全表 toArray | 中 |
| 6 | P3 | snapshot 无上限 | 中 |
| 7 | P4 | hub writeFileSync | 低 |

---

## 8. 验证基线

| 项 | 结果 |
|---|---|
| `npm test` | **799 passed / 0 failed**（i18n 基线需 `--js-update-baseline` 重锚） |
| 工作区 | 2 个未提交改动（fsrs.js + scheduling.js H-1 修复） |
| 本轮是否改代码 | **否**（纯审查交付） |
| 七轮累计审查 | ~70 个源文件，18 项发现，16 项已修复 |
