# round27 迭代审查报告（2026-09-07 17:2x）

> 时机说明：审查时工作区含**平行会话的 F 系列未提交改动**（10 文件 +230/−123，全部带
> `审计 F-n` 标记），本轮聚焦该"最新迭代输入"做**预提交审查**；全量套件基线 = 上一干净
> HEAD（f8767f4，931/931）。

## 一、本次迭代（F 系列，in-flight）审查结论：整体合格，3 个关注点

| # | 位置 | 内容 | 审查结论 |
|---|---|---|---|
| F-1 | `repo.js updateCard` | get→put 包进 rw 事务 | ✅ 正确且必要：避免与 review() 的 SRS 写并发时整行回滚。**关注点①**：`fireHook('onCardSaved')` 现在在事务内调用——若某插件 hook 同步开事务会嵌套报错；需确认 hook 侧对"事务内触发"有降级（repo.review 同款模式已有先例，风险低） |
| F-5 | trash/prune | 索引查询替代全表 filter | ✅ 正向（trash 有 deletedAt 索引） |
| B8/F-8 | `markFamiliar` 取消熟词 | 重置 `fsrs.last=now` | ✅ 语义正确：防 365 天跨度使 predR≈0 → 遗忘项异常放大（与 round17 卡侧修复同根） |
| F-9 | srs 考试窗口 | 弹性顺延回钳 `r.dueAt<=examAt` | ✅ 边界正确：窗口压缩不再被顺延击穿。`now` 参数贯通（A-7 时间注入落地） |
| F-11/12/14 | WordReview timer / Review 竞态 | 卸载清 timer + seq 后发先至守卫 | ✅ 标准做法 |
| F-28/29 | speak 监听/Safari 超时 | addEventListener 替代属性覆盖 + 30s 兜底 | ✅ |
| F-32 | WordReview mount | catch 降级 | ✅ |
| F-34 | debounce this | `function` + `apply(this)` | ✅ 兼容组件方法场景。**关注点②**：Vue `watch` 直接调用回调时 `this` 为 undefined，`apply(undefined)` 在严格模式下即 `fn()`——不影响现有 `watch(q, debounce(load,220))` 用法（load 不依赖 this） |

**关注点③（唯一值得跟踪的回归窗口）**：F-1 把整张卡的 `put` 移入事务后，若 `validateCard`
或任一字段在并发 review 落盘后被本路径覆盖（比如用户编辑对话框基于旧快照保存），仍会回滚
SRS——事务解决的是"读改写原子性"，不解决"UI 基于陈旧数据提交"。建议后续与 D2 的刷新信号
配套做"保存前比对 updatedAt"的乐观守卫（已在 round26 D2 语境中列为后续项）。

## 二、累积待办全景（触发条件仍有效）

| 项 | 类别 | 触发/状态 |
|---|---|---|
| 墓碑 GC 与体积上限 | 数据协同 | 无活 bug；列表随使用膨胀（健康项） |
| 字段级 LWW（同卡不同字段并发） | 架构 | 用户投诉/协作功能上线后重启（round25 已定重启条件） |
| 删除路径"四件套" helper | 架构 | 下一次新增"有关联"表之前收口（4 条复杂路径优先） |
| 乐观并发守卫（保存前比对 updatedAt） | 数据协同 | 与 F-1 事务化配套推进（见关注点③） |
| el-dialog/dvh 收尾至其余视图 | 移动端 | App.vue/AgentWorkbench/Mindmap 仍为 100vh（低危） |

## 三、建议动作
1. 等平行会话提交 F 系列后，跑全量 931+ 套件 + build + i18n 双闸做回归确认（本次迭代含
   transaction 语义变化，回归门槛按项目纪律走）。
2. 关注点①确认 hook 侧事务内触发降级；关注点③与 D2 合并立项。
