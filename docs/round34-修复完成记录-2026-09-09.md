# round34 修复完成记录（2026-09-09）

> 审查报告：`docs/round34-深度审查报告-2026-09-07.md`（34 项：1 Critical / 4 High / 16 Medium / 6 Low）
> 修复提交：`175b6f4`（36 文件 +793/-181），已推送 origin/main（16cc588..175b6f4）
> 验证：npm test **964/964**（含 i18n 双闸 + dep-check）· ESLint 0 error · vite build ✓

---

## 1. Critical / High（防崩防丢）

| # | 修复 |
|---|---|
| C1 | `reviewHistory` bulkGet 结果先 `.filter(Boolean)` 再建 Map——删卡后翻「已背记录」不再 `undefined.id` 崩溃 |
| H1 | 非卡片 `updatedAt` 表（notes/memos/plans/mindmaps + linkedCardIds）引入 **fieldTs 字段级合并**（`mergeByFieldTs` helper：双端皆带 fieldTs 才启用，向后兼容，docs/exams 等生成表零回归） |
| H2 | `remapCardRefs` 末尾对正文调 `remapWikilinks`——导入去重保留旧卡时 `[[c-]]/[[d-]]` 双向链接重定向，不再悬空 |
| H3 | `createWordCard`/`updateWordCard` 入口 `plain()` 去 Vue 响应式 Proxy——根绝间歇 DataCloneError（AI 自动填充后保存必现） |
| H4 | `dashboardSnapshot`/`failCountMap` 缓存键加 `currentDbMode()|` 前缀——演示/真实库撞键不再互漏数据 |

## 2. Medium（数据正确性 / 同步 / 数值 / 建模）

- **M1** 错因合并改「清空优先」：本地 WRA ≥ 远端即维持清空（原 `+1` hack 会被远端真实时间戳击败）
- **M2** 通用导出（卡片/笔记/导图/资料）时间统一本地 `YYYY-MM-DD HH:mm`（`fmtLocal`），修 UTC 差一天
- **M3** 新增 `pruneTombstones`：30 天 TTL + 按 kind bulkGet 查本地残留行才 GC；墓碑/同步包不再随删除无限膨胀
- **M4** 新增 `pruneAiUsage`(90d) / `prunePrivacyRecords`(180d) 本地裁剪，随 sync 导入完成段触发
- **M5** 删卡/删词卡的孤儿图清理同步删 `imageRefs` 反向索引悬空行
- **M6** `deleteNote` 补孤儿图片清理（findOrphanImages + 墓碑 + 索引），与 deleteCard 口径对齐
- **M7** `dueWordCards` 补并查缺失 `dueAt` 的幽灵词卡（索引查询天然漏 undefined，永不入队）
- **M8** `importBackup` 词卡去重前 `db.wordCards.toArray()` 防御式 try/catch（缺表不 abort 导入）
- **M9** `wipeUserOps` 一键清空改为「清本地 + 写 `userOp` 墓碑」——水位是设备本地的、不跨设备，仅靠 `filterClearedRows` 无法防对端复活；墓碑经 importBackup cascade 阶段传播
- **M10** `BACKUP_VERSION` 7→8（schema 演进必 bump），hub-safety 测试随包更新
- **M11** FSRS `lossOf` init 分支（reps=0）不计首测伪造 R≈1 的 loss
- **M12** `stabilityAfterRecall` 内部钳 D∈[1,10]——未 clamp 的 D>11 不再把间隔锁死到 0.1
- **M13** `easyBonus`(w16) 钳 ≥1——训练器可拟合到 <1，grade 4「简单」不得弱于「良好」
- **M14** hub MIME 表补 `.wasm: application/wasm`（instantiateStreaming 需要）
- **M15** `edgesToForest`：onPath 回边检测（必须放子循环内，visited 短路会吞回边）→ `hasCycle` 透传导图/树视图；全成环时按入度升序选锚点
- **M16** `analytics.correctRate` 计 rating≥1——「模糊」不再算 0% 掌握

## 3. Low

- **L1** `mapLabelsToCards` byLabel 改存数组，同 front 多卡不再静默丢卡
- **L2** 薄弱队列同优先级 tie-break 改难卡优先（`(b.difficulty - a.difficulty)`）
- **L3** forecast 返回补 `backlogIncludedInToday: true` 结构化标记（byDay[0] 已含 backlog，防消费者双计）
- **L4** srs 弹性顺延后末位回钳点从 `examAt` 改 `examAt - EXAM_BUFFER_DAYS·DAY`（scheduling.js 导出共享常数 0.5）——休息日介入不再丢半天缓冲
- **L5** `getStreak` 400 天扫描窗口封顶行为注释注明（UI 建议标注「近 400 天连续打卡」）
- **L6** `PAIR_MAX=400` 截断取舍文档化（>400 卡各 ≥1 错时低频真实易混对可能落选，属有意的算力护栏）

## 4. 额外修复 / 卫生

- **自引入回归**：`updateNote` 的 H1 编辑误用未定义变量 `out`（应为 `norm`）→ 崩溃级，已修并归位
- **i18n 双基线重锚**：js 411 行 / view 179 行（位移误报 70+13 行经指纹核验全部为 HEAD 已收录内容，零真新增），baseline json 一并提交
- **新增回归测试**：M12/M13 fsrs 钳制、M15 hasCycle（环真报 + DAG 不误报）、L3 `backlogIncludedInToday`、L4 弹性半天缓冲、BACKUP_VERSION=8
- 测试基线 961 → **964**（+3 净增）

## 5. 备注

- dist 清空被沙箱 safe-delete 拦截 → `mv dist dist_bak_20260909_221149` 绕过后重建成功（bak 已 gitignore）
- 遗留（非本轮范围）：10 插件沙箱 CSP、17 App.vue SettingsPanel 拆分、BUG-02 graphAuto Worker 化、BUG-07 image 反向索引性能债
