# SxyBrick 审计 · round25（项目更新后第五轮深度审查）

> 日期：2026-09-03（项目自 round24 后新增3 个提交）
> 基线：HEAD `71bd10d`（fix(english)），792 passed / 0 failed
> 方法：新提交逐条 diff 审计 + round21~24 遗留项回归扫描 → 主审实证复核
> 结论：本轮**未修改任何代码**（纯审查交付）。

---

## 1. 新提交审查（3 个，自 round24 后）

### e64d063 · fix(p2): 性能与体验（M10/M11/M12/E3/E4/M9 + PWA/文案）

- **M10 embeddings 全表载入**：新增 `FULLSCAN_ROW_LIMIT=3000`，超限显式报错。semantic_search / retrieve_context 工具补 try/catch。✅
- **M11 listCards 双扫描**：全量只读一次，dueCount 复用同一批数据。旧实现每次列表 2 次全表扫描。✅
- **M12 importBackup JSON.stringify**：新增 `sameShape` 快速淘汰（键数 + 时间水位），仅罕见兜底才 JSON.stringify。✅ **round21 M-6 遗留项已修复。**
- **E3 Cards.vue 分页**：page 视图真分页 50 条/页 + 页码条 + 越界自动收回，万卡不再一次性建 1 万个 DOM。✅
- **E4 ForgettingCurve 泄漏**：resize 监听移入 onMounted/onBeforeUnmount，dispose+置 null。✅ **round22 L-5 关联项已修复。**
- **M9 LibraryFiles 预览泄漏**：releaseImgUrl/releasePdf 统一释放（切换/关闭/卸载三处）。✅
- **PWA/文案**：manifest 补 lang zh-CN、图片 lazy 加载、Dashboard 错误兜底+重试、文案清理。✅
- **结论**：质量高，6 项性能/泄漏修复全部到位，测试覆盖。

### 71bd10d · fix(english): 背诵题干答案倒挂 + 例句收敛 + 拍照识字

- **pickMeaning 方向修复**：choice/listenChoice/quiz 看词选义、reverse/englishEnglish/collocations 看义选词（此前反向导致题干=答案）。✅
- **spell 比对方向**：改为比对中文释义而非英文单词，输入框提示同步修正。✅
- **例句档位收敛**：由 4 档(simple/long/en1/en2)收敛为 2 档(simple/long)。✅
- **仅填单词自动生成**：save 时检测 isWordOnly()，AI 开启且大纲内则自动补齐同义词/词组/短语/例句。✅
- **拍照识字 OCR**：WordBook 新增📷入口 → ocrImageText → extractWords → 批量建卡+自动补全。✅
- **结论**：实现完整，测试覆盖 pickMeaning/spell 方向断言 + 例句档位断言。

### 76f46af · docs(audit): 审计闭环报告

- 纯文档提交（审计修复闭环报告 + 判定项说明），无代码变更。✅

---

## 2. round21~24 遗留项回归扫描

| 编号 | 问题 | 状态 | 说明 |
|---|---|---|---|
| **H-1** | desiredRetention `\|\|` → `??` | **未修复** | fsrs.js:156 + scheduling.js:47 仍用 `\|\|` |
| **H-2** | graphEdges 缺 fromCardId 索引 | **未修复** | db.js:50 仍为 `id, from, to, updatedAt` |
| **H-3** | pomoSessions 缺 partial 索引 | **未修复** | db.js:56 仍为 `id, startedAt` |
| H-4 | importBackup 单事务锁 | 未修复 | — |
| **M-6** | JSON.stringify 逐卡比对 | **✅ 已修复** | e64d063 sameShape 快速淘汰 |
| L-5 | analytics 全表 toArray | 未修复 | — |
| P1~P6 | round22 性能项 | 部分修复 | M10/M11/M12/E3/E4/M9 已修复 |

---

## 3. 本轮新增代码质量评价

- **性能修复**（M10~M12）：全库检索上限、listCards 单扫描、importBackup 快速淘汰——三项修复精准命中 round21/22 发现的性能瓶颈
- **泄漏修复**（E4/M9）：ForgettingCurve resize 闭包泄漏、LibraryFiles PDF/Blob URL 泄漏——两处资源泄漏修复到位
- **英文模块**（fix english）：pickMeaning 方向修复解决了一个影响用户体验的核心 bug（题干=答案），OCR 集成完整
- **整体质量**：代码审查覆盖率充分，测试从 790 增长到 792

---

## 4. 五轮累计汇总（round21~25）

| 轮次 | 日期 | 发现数 | 已修复 |
|---|---|---|---|
| round21 | 2026-09-02 | 4高+5中+4低 | 0 |
| round22 | 2026-09-02 | 6项新增 | 0 |
| round23 | 2026-09-02 | 1项新增 | 0 |
| round24 | 2026-09-03 | 1项需关注 | 0 |
| round25 | 2026-09-03 | 0项新增（M-6 已修） | 1（M-6） |
| **合计** | | **18 项** | **1 项已修复** |

**累计已修复项：**
- M-6（JSON.stringify 逐卡比对）→ e64d063 sameShape 快速淘汰
- E4（ForgettingCurve resize 泄漏）→ e64d063 onMounted/onBeforeUnmount
- M9（LibraryFiles 预览泄漏）→ e64d063 releaseImgUrl/releasePdf
- M10（embeddings 全表载入）→ e64d063 FULLSCAN_ROW_LIMIT=3000
- M11（listCards 双扫描）→ e64d063 单次全量读取
- R4（复习评分重入锁）→ 8e8f350 ratingBusy
- R2（全局错误守卫）→ 8e8f350 window error + ErrorBoundary
- R3（IndexedDB 故障可见性）→ 8e8f350 状态机 + 横幅
- S1（CSV 公式注入）→ 8e8f350 sheetCellGuard
- M3（Hub 损坏恢复）→ 95a90a9 recoverCorrupt
- M4（Hub 版本校验）→ 95a90a9 version check
- M7（Hub 隐私过滤）→ 95a90a9 GET 默认不含 privacyRecords
- M5（deleteCard 级联补全）→ 95a90a9 notes + 图片墓碑
- M14（加密备份）→ 580b1fc AES-GCM + PBKDF2

**剩余未修复项（3 高 + 若干中低）：**

| 优先 | 编号 | 问题 | 成本 |
|---|---|---|---|
| 1 | H-1 | `\|\|` → `??` 语义修复 | 极低（一行×2） |
| 2 | H-2 | graphEdges fromCardId 索引 | 低（schema v25） |
| 3 | H-3 | pomoSessions partial 索引 | 低（schema v25） |
| 4 | H-4 | importBackup 单事务 | 中高 |
| 5 | L-5 | analytics 全表 toArray | 中 |

---

## 5. 验证基线（round25 审查时点）

| 项 | 结果 |
|---|---|
| `npm test` | **792 passed / 0 failed** |
| 工作区 | 干净（仅未跟踪审计报告文件） |
| 本轮是否改代码 | **否**（纯审查交付） |
| 五轮累计审查 | ~60 个源文件，18 项发现，1 项已修复 |
