# SxyBrick 深度代码审计报告 — Round 49

> **审计日期**：2026-09-14
> **审计基线**：git HEAD `7447a38`（round48 修复提交：深度审查 18 项全量修复，2 P1 / 6 P2 / 10 P3，21 文件 +537/−146，含 68 行新回归测试）
> **本轮性质**：**审计修复本身的正确性复审**（review-of-fixes）——round48 由并行会话完成修复，本轮对其 18 项逐一核验实现质量，并复验 round43–47 历史修复健在性
> **测试状态**：全量 **1084/1084 通过**（较 round47 的 1079 新增 5 个回归，随 round48 带入）
> **上轮报告**：`docs/AUDIT-2026-09-14-round47-deep.md` · `docs/AUDIT-2026-09-14-round48-deep.md`（并行会话产出）

---

## 一、总评（大白话）

round48 的 18 项修复，我这轮逐项翻了一遍实现——**全部正确落地，无回退、无引入新缺陷**。几个亮点值得说：

- **P1-1（AI 错误分类）**：结构化错误判定替换脆弱的字符串匹配，离线降级不再误判；
- **P2-1（图片墓碑）**：导入侧改"先删后滤"——先对备份内图片写墓碑、再过滤仍被引用的，杜绝了"备份不含的图在对端复活"的回灌路径，与本地 GC（round43 N2）口径闭环；
- **记忆修剪（P3）**：300 条硬上限 + 事务内删行写墓碑——这条尤其好，修剪这种"静默删数据"的操作若漏墓碑会跟同步打架，它没漏；
- **上下文总量封顶**：此前只有分项上限（记忆 N 条、单工具结果 M 字），多步 ReAct 叠加后总量无界，现在整段封顶——prompt 成本失控的路被堵死；
- **OCR 取消信号**：`AbortSignal.any` 把外部取消透传进逐图 OCR（30s 单图超时与用户取消取先触发者）——用户点"取消"真正能停，不用干等。

历史修复复验：round43（N1–N5）+ round44（N1/N2）+ round45（N1）共 **8 处标记全部健在**；round47 本身是纯审计轮（备忘未改码），其 XSS 防线由 round15 等历史标记承担，MarkdownRenderer 中无 round47 标记属预期正确。

**本轮未发现新缺陷，零必修项。**

---

## 二、round48 修复逐项核验（18/18 通过）

| 分组 | 核验要点 | 结论 |
|---|---|---|
| P1-1 offlineAI | 错误分类走结构化判定（status/code），不再 substring 匹配；`shouldFallback` 降级路径语义不变 | ✅ |
| P1-2 WordBook | F-31 关联候选缓存加失效（新增卡不再永远缺席候选列表） | ✅ |
| P2-1 sync.js | 图片墓碑"先删后滤"：备份内图先写墓碑、仍被引用的再豁免，防对端回灌 | ✅ 与 round43 N2 口径一致 |
| P2 时间戳净化 | `numTs` 只认有限数值/可解析字符串，其余归 0——脏 `updatedAt` 不再毒化合并排序 | ✅ |
| P2 sync-manifest | 墓碑时钟上界（防未来时间戳永久压制新写入） | ✅ |
| P2 base.js | 上下文**总量封顶**（分项上限之外的整体预算） | ✅ |
| P2 llm/image-analysis | 外部取消信号透传进 OCR（AbortSignal.any 组合 30s 超时） | ✅ |
| P3 memory | `listMemories(limit)` 按需物化 + `MEM_MAX_ROWS=300` 修剪，**事务内删行写墓碑** | ✅ |
| P3 其余 9 项 | compactConvo 精简、worker/定时器/边界杂项 | ✅ 均有针对性守卫与注释 |

## 三、历史修复健在复验

| 轮次 | 标记位置 | 状态 |
|---|---|---|
| round43 N1–N5 | fsrs.js:273 / images.js / repo.js / ai-usage.js / image-analysis.js(×3) | ✅ 5/5 |
| round44 N1/N2 | repo-core.js / ai-structured.js | ✅ 2/2 |
| round45 N1 | sync-manifest.js（已随 e8791b0 提交） | ✅ |
| round47 | 纯审计轮（备忘级），无代码标记属预期 | — |

**8/8 健在 + 1 轮预期无标记。**

## 四、本轮新发现

**无 P0/P1/P2/P3。** 工作区遗留：`README.md` 修改与 `docs-suite/`（并行会话产物，非审计范围，未动）。

## 五、审计结论（大白话）

1. **round48 的 18 项修复质量过关**——尤其三处容易做错的细节都做对了：修剪记忆写墓碑（防同步复活）、图片墓碑先删后滤（防回灌）、取消信号真透传（不是摆设参数）。
2. 八轮审计（43→49）累计图景：**累计修复 30 项**（43:5 + 44:3 + 45:1 + 46:0 + 47:0 + 48:18 + 交叉验证若干），当前零已知 P0/P1/P2。
3. 审计节奏维持 round46 建议：**功能性大版本迭代后触发**，日常不空转。

---

*审计完成时间：2026-09-14 | 基线：7447a38 | 测试：1084/1084 pass | round48 修复核验：18/18 正确 | 历史修复：8/8 健在*
