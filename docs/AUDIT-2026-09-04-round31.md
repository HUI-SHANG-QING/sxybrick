# SxyBrick 审计 · round31（自 round30 无变更，四维度全量重审）

> 日期：2026-09-04
> 基线：HEAD `facffb5`，800 passed / 0 failed
> 结论：自 round30 以来零变更，四维度重审未发现新缺陷。代码库状态稳定。

---

## 1. 取证

自 round30（`facffb5`）以来：无新提交、无未提交改动、测试 800/800。本轮为纯深度重审。

## 2. 算法维度

| 模块 | 审查结论 |
|---|---|
| fsrs.js stabilityAfterRecall | ✅ Math.exp(w[7]) 溢出由 MAX_STABILITY clamp 兜底 |
| fsrs.js stabilityAfterForget | ✅ (s+1)^w[13] 极端值由 Math.max(0.01) 兜底 |
| fsrs.js retrievability | ✅ clamp 输入 s≥0.01, t≥0 |
| fsrs.js nextDifficulty | ✅ clamp(reverted, 1, 10) |
| fsrs.js trainWeights | ✅ 有限差分梯度 + plateau 机制 + weight≥0.01 |
| srs.js computeNext | ✅ level 归一化防 NaN，短期巩固状态机闭环 |
| forecast.js | ✅ 同卡同天去重 + MIN_STEP_MS + MAX_SAME_DAY_STEPS |
| calibration.js | ✅ Brier/ECE 公式正确 |
| session.js | ✅ 贪心+邻接惩罚 |
| adaptive-retention.js | ✅ 线性插值+noData 守卫 |
| networth.js | ✅ isReviewed 防 NaN |

## 3. 业务逻辑维度

| 检查项 | 结论 |
|---|---|
| repo.js review 异常路径 | ✅ throw on 卡片不存在，事务原子性 |
| repo.js deleteCard 级联 | ✅ notes/graphEdges/embeddings/images 墓碑全覆盖 |
| wrongReason 清空 | ✅ clearedReason 时 bump wrongReasonAt |
| isPomoCountable 四端一致 | ✅ countPomoToday + achievements + analytics + WeeklyReport |
| word-repo.js reviewWord | ✅ 复用 scheduleReview，FSRS/SM-2 自动切换 |
| WordReview.vue 13 模式 | ✅ showQWord/pickMeaning/quiz 随机方向一致 |
| sync.js importBackup | ✅ sameShape 快速淘汰 + 加密备份 |

## 4. 数据协同 + 数据对象维度

| 检查项 | 结论 |
|---|---|
| tombstones 覆盖 | ✅ card/memo/note/plan/doc/mindmap/wordCard/groupLink/graphEdge/review/image/dailyPlan/dailyTask 全覆盖 |
| schema v29 | ✅ graphEdges fromCardId/toCardId + pomoSessions roundId + embeddings sourceId 前置 |
| SYNC_TABLES vs db schema | ✅ 一致 |
| EXCLUDED_FROM_SYNC | ✅ wordStudyLog + notifications/errors/snapshots/plugins/aiUsage |
| EXT_FIELDS vs WORD_EXT_FIELDS | ✅ derived 双重保护 |

## 5. 全局交叉结论

- **缺陷**：无新发现
- **优化**：已知项（P1 analytics 全表 toArray / P3 snapshot 无上限 / P4 hub writeFileSync）仍未修复，优先级不变
- **扩展**：无新扩展点

## 6. 十轮累计汇总

| 轮次 | 日期 | 发现数 | 已修复 |
|---|---|---|---|
| round21~30 | 2026-09-02~03 | 18项+3测试修复 | 全部 |
| round31 | 2026-09-04 | 0（零变更重审） | — |
| **合计** | | **18项+3测试修复** | **全部已修复** |

---

## 验证基线

| 项 | 结果 |
|---|---|
| `npm test` | **800 passed / 0 failed** |
| 工作区 | 干净 |
| 本轮是否改代码 | 否 |
